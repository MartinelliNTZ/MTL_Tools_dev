2.2.1.6

- Reorganizada interface do SettingsPlugin: adicionados dois widgets colapsaveis - "Geral" (botao de preferencias, idioma, precisao de campos vetoriais, limiar assincrono) e "Plugin de Calculos Vetoriais" (metodo de calculo vetorial) para melhor organizacao da UI.

2.2.5.1

- Corrigido bug de inicializacao de log em `LogUtils`: anteriormente usava `Path.cwd()` (ex: `C:\Windows\system32`) quando nao inicializado, causando `PermissionError` ao criar pasta `log`.
- Implementado fallback seguro para `plugin_root` via `Path(__file__).resolve().parents[3]` e, se necessario, `Path.home()/Cadmus/log`.
- Ajustado `LogUtils._write_event` para continuar sem arquivo de log e enviar mensagem por `QgsMessageLog`/stderr em casos de falha de criacao de arquivo.

2.2.5.2

- Adicionados no `SettingsPlugin`, dentro de `calc_collapsable`, dois campos configuraveis inicialmente para nomes de area cartesiana e elipsoidal com `WidgetFactory.create_input_fields_widget`.
- As preferencias desses campos passaram a ser carregadas e salvas em `ToolKey.VECTOR_FIELDS`.
- Incluida validacao para impedir salvamento quando os dois valores configurados forem iguais, exibindo alerta com `QgisMessageUtil`.

2.2.5.3

- Ajustados os dois campos do `SettingsPlugin` para representar sufixos, nao nomes completos de campo: `Sulfixo Cartesiano` com padrao `""` e `Sulfixo Elipsoidal` com padrao `"_eli"`.
- As preferencias em `ToolKey.VECTOR_FIELDS` passaram a usar as chaves `cartesian_suffix` e `ellipsoidal_suffix`.
- Mantida a validacao para impedir salvamento quando os dois sufixos forem iguais, agora com mensagem alinhada ao novo comportamento.

2.2.5.4

- Implementada a leitura dos sufixos configurados em `ToolKey.VECTOR_FIELDS` no `VectorFieldsCalculationPlugin`.
- Os calculos de `length` para linhas e `area` para poligonos agora usam os sufixos configuraveis ao resolver os nomes dos campos.
- O comportamento de pontos (`x` e `y`) foi mantido sem alteracoes.

2.2.5.5

- Corrigida chamada de leitura dos sufixos no `SettingsPlugin` para compatibilidade com `InputFieldsWidget.get_value`, evitando erro em runtime ao salvar as preferencias.

2.2.5.6

- Adicionada nova ferramenta de dialogo `VectorToSvgPlugin` na categoria `FOLDER`, registrada no `ToolRegistry` com `cadmus_icon` e exibicao em toolbar e menus.
- Implementada a UI inicial do `VectorToSvgPlugin` herdando de `BasePluginMTL`, composta apenas no `_build_ui()` com `WidgetFactory.create_layer_input`, tres `create_color_button` e um `create_checkbox_grid`.
- Registrado novo `ToolKey.VECTOR_TO_SVG` para logging e identificacao da ferramenta.

2.2.5.7

- Corrigido import de `QgsMapLayerProxyModel` no `VectorToSvgPlugin`, trocando `qgis.gui` por `qgis.core` para compatibilidade com QGIS 3.16.

2.2.5.8

- Refinado o layout do `ColorButtonWidget` para melhor alinhamento horizontal entre titulo, seletor de cor, input hexadecimal e botao de copiar.
- Removida a largura fixa pequena do titulo na `WidgetFactory.create_color_button`, reduzida a altura visual do input hexadecimal e ajustado o botao de copiar para respeitar melhor os tamanhos configurados.
- Simplificado o estilo de `Styles.color_button_widget()` para evitar que o QSS force alturas e tamanhos minimos conflitantes com a configuracao aplicada pela factory.

2.2.5.9

- Padronizados novos métodos de `QgisMessageUtil`: `ask_overwrite`, `show_styled_message_bar`, `show_progress_message_bar`, `update_progress`, `clear_message_bar`, `show_level_message_bar` e `show_message_bar_with_button` foram convertidos em métodos estáticos.
2.2.5.10

- Implementada a execucao completa do `VectorToSvgPlugin`, com exportacao de SVG unico ou um SVG por feicao, respeitando `somente feicoes selecionadas`, fundo transparente, exibicao opcional de borda e rotulo, e abertura do resultado na pasta de saida.
- A exportacao agora usa `ProjectUtils` para resolver pasta do projeto e validar a camada, `VectorLayerGeometry.get_selected_features()` para materializar apenas a selecao quando o checkbox estiver ativo e mantem o renderer da camada original para desenhar as geometrias no SVG.
- Adicionada a regra de nomeacao para multiplos SVGs: usa o campo `Name` quando existir e tiver valor; caso contrario, usa o nome da camada do QGIS com sufixo incremental `_1`, `_2`, `_3`...
- Incluida persistencia da opcao `Gerar SVG para cada feicao` e ajuste do plugin para salvar os arquivos na pasta escolhida com nomes sanitizados e sem sobrescrever arquivos existentes.

2.2.5.11

- Criado o utilitario generico `SVGUtils` em `utils/SVGUtils.py` para concentrar responsabilidades reutilizaveis de SVG: preparacao de feicoes, montagem do documento SVG, conversao de geometrias em elementos SVG, extracao de estilo do renderer, sanitizacao de nomes e escrita de arquivos.
- Refatorado o `VectorToSvgPlugin` para manter apenas a orquestracao da ferramenta e delegar ao `SVGUtils` as responsabilidades genericas de geracao de SVG.
- Mantido o comportamento funcional da exportacao anterior, reduzindo acoplamento do plugin e facilitando reutilizacao futura por outras ferramentas do projeto.

2.2.5.12

- Corrigida a semantica de `Mostrar Borda` na exportacao SVG: a opcao agora controla o contorno real das geometrias vetoriais e nao mais um retangulo ao redor da dimensao total do SVG.
- Implementado suporte a rotulos reais da camada no `SVGUtils`: quando a camada tiver rotulagem habilitada no QGIS e `Mostrar Rotulo` estiver marcado, os textos das feicoes passam a ser desenhados no SVG nas respectivas geometrias.
- Mantido o comportamento de ocultar completamente os rotulos quando o checkbox correspondente estiver desmarcado.

2.2.5.13

- Reimplementada a abordagem de borda e rotulo no `SVGUtils` com base na API de labeling do QGIS: os rotulos agora sao lidos a partir de `layer.labeling().settings()` e avaliados por expressao/campo com `QgsExpression`.
- Ajustado `Mostrar Borda` para controlar diretamente o stroke das geometrias exportadas, usando a cor de borda configurada na ferramenta em vez de desenhar moldura externa.
- Removidos do SVG o retangulo de contorno global e o rotulo de rodape generico, substituindo-os por rotulos posicionados nas proprias feicoes quando a camada tiver labeling ativo.

2.2.5.14

- Tornada mais robusta a exportacao de borda no `SVGUtils`: a largura do contorno agora e definida explicitamente por tipo de geometria, evitando depender de propriedades de `symbolLayer` que variam entre simbolizadores.
- Adicionado fallback de rotulos por `customProperty` da camada (`labeling/fieldName`, `labeling/isExpression`, `labeling/fontFamily`, `labeling/fontSize`) para aumentar compatibilidade entre versoes e implementacoes de labeling do QGIS.
- Implementado fallback de leitura de settings de labeling para assinaturas diferentes de `settings(...)`, cobrindo cenarios em que a API exige apenas `providerId` ou `layer, providerId`.

2.2.5.15

- Adicionado ao `VectorToSvgPlugin` um controle de `Espessura Borda` via `WidgetFactory.create_double_spin_input`, com persistencia nas preferencias da ferramenta.
- Conectada a espessura configurada pelo usuario ao `stroke-width` final das geometrias no `SVGUtils`.
- Expandido o fallback de rotulos no `SVGUtils` para tentar `displayExpression()` e, por ultimo, o campo `Name`, com logs de debug para facilitar diagnostico quando o rotulo continuar vazio.

2.2.5.16

- Adicionado ao `VectorToSvgPlugin` um controle de `Tamanho Rotulo` na interface, com persistencia nas preferencias da ferramenta.
- Ajustado o `SVGUtils` para priorizar o tamanho de rotulo configurado pelo usuario no plugin sobre o tamanho vindo da configuracao de labeling da camada.

2.2.5.17

- Substituidas no `VectorToSvgPlugin` as principais strings hardcoded por `STR`, reaproveitando termos existentes quando o significado era equivalente.
- Adicionadas ao `Strings_pt_BR.py` apenas as chaves especificas do `VectorToSvgPlugin` que nao possuíam equivalente claro no catalogo atual.

2.2.5.18

- Generalizadas as mensagens de exportacao do `VectorToSvgPlugin`, reaproveitando ao maximo strings ja existentes do catalogo `STR`.
- Removidas do `Strings_pt_BR.py` as chaves especificas de erro/sucesso de SVG que podiam ser substituidas por termos mais amplos como `ERROR`, `EXPORT` e `LAYER_HAS_NO_FEATURES`.

2.2.5.19

- Criados os arquivos de instrucoes do `VectorToSvgPlugin` seguindo o padrao do `ToolKey.VECTOR_TO_SVG`: `resources/instructions/pt_BR/vector_to_svg_help.md`, `resources/instructions/en/vector_to_svg_help.md` e `resources/instructions/es/vector_to_svg_help.md`.
- Criado o documento de analise de cenario `docs/analise_de_cenario/vector_to_svg_analise_cenario.MD`, alinhado ao comportamento atual da ferramenta.

2.2.5.20

- Adicionados os arquivos faltantes de instrucoes para `ja` e `de`, mantendo o padrao `vector_to_svg_help.md` alinhado ao `ToolKey.VECTOR_TO_SVG`.

2.2.5.21

- Corrigido o arquivo `resources/instructions/ja/vector_to_svg_help.md`, que estava em ingles, substituindo o conteudo por uma versao efetivamente traduzida para japones.

2.2.5.22

- Refatorado o `SelectorWidget` para encapsular o botao "Usar pasta do projeto", agora habilitado via parametro generico `path_button` em `WidgetFactory.create_path_selector`.
- Atualizado o `VectorToSvgPlugin` para usar `create_path_selector(..., path_button="svgs")`, removendo o botao manual e o metodo local que montavam a pasta do projeto fora do widget.

2.2.5.23

- Aplicada a mesma refatoracao no `ExportAllLayouts`, agora usando `create_path_selector(..., path_button="exports")` em vez de manter um botao manual e um metodo local para preencher a pasta do projeto.

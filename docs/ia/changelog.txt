2.3.8 2026/04/11
[2.3.8.37.7] RasterLayerProjection: implementado `reproject_raster_to_crs(...)` usando `gdal:warpreproject`, com logging padronizado por `ToolKey`, suporte a `nodata_value`, `output_data_type=0` (usar tipo do raster de entrada), saida temporaria GeoTIFF e normalizacao de CRS de origem/destino.
[2.3.8.37.8] RasterLayerProjection: `reproject_raster_to_crs(...)` passou a usar `ResamplingMethod` (`core/enum/ResamplingMethod.py`) com padrao `CUBICO_SUAVIZADO`; `target_resolution` agora e opcional e, quando `None` ou `0`, desabilita reamostragem e nao envia `RESAMPLING` para o warp.
[2.3.8.37.9] RasterLayerProjection: implementado `get_raster_crs(...)` com suporte a `QgsRasterLayer` e caminho de arquivo, retornando `QgsCoordinateReferenceSystem` valido quando disponivel. O helper de logger da classe tambem foi corrigido para usar `class_name="RasterLayerProjection"`.
[2.3.8.37.10] RasterLayerProjection: `reproject_raster_to_crs(...)` expandido para aceitar `context`, `feedback` e `is_child_algorithm`, permitindo reutilizacao segura dentro de algoritmos Processing filhos sem duplicar infraestrutura de warp/reprojecao.
[2.3.8.37.11] RasterWeightedAverage: removido o metodo legado `_resample_rasters()` e centralizada toda a preparacao de raster em `RasterLayerProjection.reproject_raster_to_crs(...)`, eliminando duplicacao de logica de `gdal:warp` no algoritmo.
[2.3.8.37.12] RasterWeightedAverage: adicionado parametro `OUTPUT_CRS` (CRS de saida opcional). O algoritmo agora resolve explicitamente o CRS alvo antes do calculo e passa a preparar as entradas conforme combinacao de CRS/resolucao solicitada pelo usuario.
[2.3.8.37.13] RasterWeightedAverage: novo fluxo de preparacao de rasters implementado em `_prepare_rasters_for_execution(...)`. Regras: se `OUTPUT_CRS` vazio e `OUTPUT_RESOLUTION` vazio/0, apenas alinha rasters com CRS diferente ao CRS do primeiro; se `OUTPUT_CRS` informado e `OUTPUT_RESOLUTION` vazio/0, reprojeta mantendo resolucao original; se `OUTPUT_RESOLUTION > 0`, todas as camadas sao reprojetadas antes da execucao, usando `OUTPUT_CRS` quando informado ou o CRS do primeiro raster quando vazio.
[2.3.8.37.14] RasterWeightedAverage: validacao e logs refinados para o novo pipeline de media ponderada raster, incluindo obtencao centralizada de CRS via `RasterLayerProjection.get_raster_crs(...)`, criacao de arquivos temporarios por raster preparado e persistencia da preferencia `output_resolution` no novo fluxo.
[2.3.8.37.15] Validacao sintatica executada com sucesso via `ast.parse` para `utils/raster/RasterLayerProjection.py` e `processing/RasterWeightedAverage.py` apos a refatoracao de reprojecao/reamostragem.

2.3.8 2026/04/11
[2.3.8.37.6] RasterWeightedAverage: CRITICAL FIX - Changed resampling from 'gdal:translate' (non-functional for resampling) to 'gdal:warp' with TARGET_RESOLUTION and RESAMPLING=3 (cubic spline). Now properly resamples rasters to target pixel size using gdalwarp engine.
[2.3.8.37.5] RasterWeightedAverage: Changed resampling algorithm from 'qgis:rasterlayerresampler' (not available in QGIS 3.16) to 'gdal:translate'. Uses OUTSIZE parameter to specify width/height in pixels. Works reliably with QGIS 3.16.16+ and GDAL 3.4.1.
[2.3.8.37.3] Preferences now include output_resolution parameter.
[2.3.8.37.2] Added _resample_rasters() method: uses processing.run() with qgis:rasterlayerresampler, creates temporary GeoTIFF files, returns resampled QgsRasterLayer objects with weights preserved.
[2.3.8.37.1] Added OUTPUT_RESOLUTION parameter and helper function for smart resampling.
[2.3.8.37] RasterWeightedAverage: Added output resolution resampling feature. Moved Raster 3, 4, Weight 3, 4 and OUTPUT_RESOLUTION to advanced parameters. When output resolution is blank, executes normal calculation. When defined, resamples all rasters to target resolution using cubic spline interpolation (GDAL) before weighted average calculation.
[2.3.8.36] RasterWeightedAverage: Fixed QGIS 3.16 compatibility error. Removed asRectF() method call (not available in QGIS 3.16 QgsRectangle). Now logs extent as string representation.
[2.3.8.31.1] ReportGenerationStep.should_run() checks for valid json_path before attempting task creation.
[2.3.8.31.2] Added logging when step is skipped due to missing json_path.
[2.3.8.31.3] ReportGenerationStep now resolves JSON path from either json_path or photo_metadata_json_path, fixing DroneCoordinates report opening.
[2.3.8.31.4] DroneCoordinates now passes iface through ExecutionContext so ReportGenerationStep can show warnings and open HTML using ExplorerUtils from the pipeline.
[2.3.8.30] Report Generation Pipeline: Fixed KeyError when json_path missing in context. Added validation and error handling for missing JSON metadata file in ReportGenerationStep.
[2.3.8.30.1] PhotoVectorizationStep.on_success() now validates json_path before setting in context and adds debug logging.
[2.3.8.30.2] ReportGenerationStep.create_task() provides clearer error message when json_path is missing.
[2.3.8.30.3] PhotoVectorizationTask._run() validates result structure and json_path presence with detailed error logging.
[2.3.8.29] Report Generation: Moved report generation from UI thread to background TASK to prevent UI freezing. Created ReportGenerationStep and ReportGenerationTask following AsyncPipelineEngine pattern.
[2.3.8.29.1] Added ReportGenerationStep class that creates ReportGenerationTask and handles HTML report opening.
[2.3.8.29.2] Added ReportGenerationTask class that wraps ReportGenerationService.generate_from_json() in cancellable background TASK.
[2.3.8.29.3] DroneCoordinates now includes ReportGenerationStep in pipeline when generate_report=True, removing UI-blocking report generation from _on_pipeline_finished callback.
[2.3.8.29.4] PhotoVectorizationPlugin now includes ReportGenerationStep in pipeline when generate_report=True, with PhotoVectorizationTask modified to skip internal report generation.
[2.3.8.29.5] PhotoVectorizationTask constructor simplified by removing generate_report parameter (always False now, report handled by separate step).
[2.3.8.26] PhotoMetadata: Simplified field mapping logic in _extract_photo_payload. Removed complex suffix-based normalization workaround and replaced with direct enum value lookup: {enum.value: enum.key} mapping. Now canonicalizes raw field names (e.g., "GPSMapDatum") to MetadataFieldKey enums directly.
[2.3.8.26.1] PhotoMetadata._extract_photo_payload now imports MetadataFieldKey locally and builds a value-to-key map on first use, avoiding unnecessary iterating over field specs.
[2.3.8.26.2] Syntax validation passed for PhotoMetadata.py.

[2.3.8.28] PhotoVectorizationPlugin: Refactored to use TASK system instead of direct service calls. Created PhotoVectorizationStep and PhotoVectorizationTask to leverage AsyncPipelineEngine infrastructure from DroneCoordinates, eliminating code duplication and improving consistency.
[2.3.8.28.1] Added PhotoVectorizationStep class that creates PhotoVectorizationTask and handles success/error callbacks.
[2.3.8.28.2] Added PhotoVectorizationTask class that wraps PhotoFolderVectorizationService.generate_from_folder() in a cancellable TASK.
[2.3.8.28.3] PhotoVectorizationPlugin now uses AsyncPipelineEngine with PhotoVectorizationStep, maintaining same UI but with TASK-based execution for better error handling and progress reporting.
[2.3.8.27] ReportMetadataPlugin refactor: removed photo folder vectorization UI from the report tool and created a dedicated PhotoVectorizationPlugin for image-to-vector generation.
[2.3.8.27.1] Added ToolKey.PHOTO_VECTORIZATION and registered a new agriculture tool in ToolRegistry.
[2.3.8.27.2] PhotoVectorizationPlugin now persists folder path and options separately from report metadata preferences.
[2.3.8.25] MetadataFields: Removed all direct REQUIRED_FIELDS usage patterns. Methods all_fields() and required_keys() now build data from EXIF_FIELDS and DJI_XMP_FIELDS instead of the union.
[2.3.8.25.1] PhotoMetadata._extract_photo_payload now maps EXIF and DJI_XMP fields separately in suffix lookup, avoiding deprecated REQUIRED_FIELDS iteration.
[2.3.8.25.2] REQUIRED_FIELDS union preserved as {**EXIF_FIELDS, **DJI_XMP_FIELDS} for backward compatibility only; internal code uses explicit dictionaries.
[2.3.8.25.3] Syntax validation passed for MetadataFields.py and PhotoMetadata.py.

[2.3.8.24] DroneCoordinates UI refactored: split single "Required Fields" collapsible into two separate sections "EXIF Fields" and "DJI Fields" (XMP) for better field organization and user clarity.
[2.3.8.24.1] MetadataFields now exposes exif_keys() and xmp_keys() methods for UI and pipeline usage.
[2.3.8.24.2] DroneCoordinates stores EXIF and XMP field selections in separate preferences (exif_fields_selected, xmp_fields_selected) with dedicated collapsible state tracking.
[2.3.8.24.3] Execute pipeline combines EXIF + XMP selections into selected_required_fields for backward compatibility with downstream PhotoMetadataTask.
[2.3.8.24.4] Syntax validation passed for MetadataFields.py and DroneCoordinates.py.

[2.3.8.23] MetadataFields: separated required field catalog into EXIF_FIELDS and XMP_FIELDS, preserving REQUIRED_FIELDS as a compatibility union.
[2.3.8.23.1] PhotoMetadata._dump_allowed_keys now uses EXIF_FIELDS/XMP_FIELDS explicitly instead of the legacy single REQUIRED_FIELDS dictionary.

[2.3.8.1]#comeÃƒÂ§e aqui










#antigo exemplo de como deve ser
2.3.6.1 2026/04/05
[2.3.6.1] SettingsPlugin: Added "Pasta de projetos" path selector (mode=folder) with save/load from preferences (projects_folder key).
2.3.6.1.1 2026/04/06
[2.3.6.1.1] SettingsPlugin/WidgetFactory: Added an exclusive DatumSelectorWidget with descriptive metadata, integrated into SettingsPlugin and persisted via the default_datum preference.
[2.3.8.2] Planejamento inicial da reimplementacao completa do DroneCoordinates: criado plano tecnico detalhado em docs/ia/PLANO_REIMPLEMENTACAO_DRONE_COORDINATES_V4.md com fases, riscos, contratos e criterios de aceite.
[2.3.8.2.1] Criado docs/ia/todo.txt com backlog operacional por etapas (E01-E09), checklist incremental e rastreabilidade da execucao.
[2.3.8.2.2] Consolidado no plano o escopo de refatoracao: PhotoMetadata como manager, absorcao de OBSOLETManager, nova UI com 3 grids de campos (REQUIRED/CUSTOM/MRK), filtros persistidos e integracao do Runner por preferencias.
2.3.8.3 2026/04/07
[2.3.8.3] Etapa E02 concluida: padronizacao tecnica das classes de metadata em utils/mrk (ExifUtil, XmpUtil, CustomPhotosFieldsUtil, OBSOLETManager) com imports de pacote Cadmus e preparacao para integracao no PhotoMetadata.
[2.3.8.3.1] ExifUtil e XmpUtil migrados para usar LogUtils/ToolKey, removidos prints de erro, mantido retorno estruturado para falhas de leitura sem interromper pipeline.
[2.3.8.3.2] CustomPhotosFieldsUtil ajustado para remover dependencia de Strings legado, adotando MetadataFields para LIGHT_SOURCE_VALUES, adicionando logging base e removendo bloco de execucao direta (__main__).
[2.3.8.3.3] OBSOLETManager reescrito em padrao de utilitario legaciado com imports relativos corretos, logging unificado, e acoplamento explicito com ExifUtil/XmpUtil/CustomPhotosFieldsUtil para compatibilidade transicional.
[2.3.8.3.4] Validacao sintatica executada com sucesso via py_compile para os quatro modulos alterados.
2.3.8.4 2026/04/07
[2.3.8.4] Etapa E03 concluida: MetadataFields recebeu novo bloco MRK_FIELDS padronizado e expansao de CUSTOM_FIELDS com campos solicitados (FileType, dt_full, dt_date, dt_time, FlightNumber, FlightName, FolderLevel1, FolderLevel2).
[2.3.8.4.1] Padrao de schema preservado em todos os novos campos: normalized/core/label/attribute/description.

2.3.8.5 2026/04/07
[2.3.8.5] Etapa E05 concluida: CheckboxGridWidget reimplementado com retrocompatibilidade de formatos (dict legado, dict expandido e lista key/label/description), suporte a tooltip por description e novos metodos de estado (checked/unchecked/state_map).
[2.3.8.5.1] WidgetFactory.create_checkbox_grid atualizado para novo contrato sem quebrar chamadas antigas com options_dict, e com opcao return_widget=True para uso avanÃ§ado.

2.3.8.6 2026/04/07
[2.3.8.6] Etapa E06 concluida: DroneCoordinates UI atualizada com 3 collapsibles de campos (Required, Custom, MRK), grids via StringAdapter e persistencia completa de selecao/expansao em preferencias.
[2.3.8.6.1] Execute_tool passou a enviar listas selecionadas no ExecutionContext (selected_required_fields, selected_custom_fields, selected_mrk_fields).

2.3.8.7 2026/04/07
[2.3.8.7] Avanco E07/E08: cadeia de filtros propagada para PhotoMetadataStep/PhotoMetadataTask e DroneCoordinatesRunner, incluindo leitura de preferencias no runner.
[2.3.8.7.1] PhotoMetadata.enrich passou a aceitar parametros de selecao de campos (ainda em modo preparatorio para etapa de filtro final).
[2.3.8.7.2] Ajuste de robustez em PhotoMetadataStep: removido uso de self.logger inexistente, substituido por LogUtils contextual.
[2.3.8.7.3] Validacao sintatica executada com sucesso via py_compile nos modulos alterados.
2.3.8.8 2026/04/07
[2.3.8.8] Etapa E04 concluida: PhotoMetadata reestruturado para papel de manager/orquestrador usando ExifUtil, XmpUtil, CustomPhotosFieldsUtil e contexto MRK, removendo responsabilidade de extracao antiga acoplada.
[2.3.8.8.1] Implementado fluxo full-dump: todos os dados de fotos sao coletados sem filtro e persistidos em JSON temporario via ExplorerUtils.create_temp_json(...).
[2.3.8.8.2] Implementado filtro final por selecao de campos (required/custom/mrk) antes de aplicar atributos nos pontos.
[2.3.8.8.3] Introduzido mapeamento de contexto de voo em ingles (FlightNumber/FlightName/FolderLevel1/FolderLevel2) mantendo chaves legadas MRK para compatibilidade.

2.3.8.9 2026/04/07
[2.3.8.9] Etapas E07/E08 finalizadas: listas selecionadas de campos agora percorrem UI -> ExecutionContext -> PhotoMetadataTask -> PhotoMetadata.enrich e tambem o DroneCoordinatesRunner via preferencias persistidas.
[2.3.8.9.1] DroneCoordinates e Runner passaram a usar extra_fields=None no parse inicial, deixando a criacao de atributos 100% dirigida pelo filtro aplicado no Step de metadata.

2.3.8.10 2026/04/07
[2.3.8.10] ExplorerUtils recebeu helper create_temp_json(...) para persistencia de arquivos temporarios de diagnostico/metadata com logging padronizado.
[2.3.8.10.1] Validacao sintatica ampliada executada com sucesso via py_compile nos modulos alterados desta rodada.
2.3.8.11 2026/04/07
[2.3.8.11] Analise de log em runtime: identificado warning de falha no calculo de CUSTOM_FIELDS por ausencia de chave esperada ('AbsoluteAltitude') e erro de aplicacao de atributo no layer por tipo nao normalizado (PyQt_PyObject).
[2.3.8.11.1] PhotoMetadata recebeu instrumentacao adicional de diagnostico (selected_keys_sample, resumo por grupo, campos ausentes pre-custom, pontos sem campos filtrados) para rastreabilidade fina da filtragem.
[2.3.8.11.2] PhotoMetadata passou a mapear aliases criticos de chaves XMP (drone-dji:*) para nomes esperados pelo pipeline/custom calculators.
[2.3.8.11.3] PhotoMetadataStep agora normaliza valores para string antes de changeAttributeValue e loga tipo/valor em caso de erro, reduzindo falhas de conversao do QGIS.
[2.3.8.11.4] PhotoMetadataTask ganhou logs extras de selecao de campos e amostra de field_names retornados para facilitar auditoria do que entrou/ficou de fora.
2.3.8.12 2026/04/07
[2.3.8.12] Correcao de consistencia de campos no fluxo DroneCoordinates: PhotoMetadataTask passou a carregar todos os atributos da feicao MRK (nao apenas foto/mrk_folder), preservando lat/lon/alt/mrk_*/flight_number/flight_name/pastas para enriquecimento e filtro final.
[2.3.8.12.1] Garantia de schema completo: PhotoMetadataTask agora inclui todas as chaves selecionadas no field_names mesmo quando o valor estiver ausente, evitando perda de colunas no output.
[2.3.8.12.2] Correcao de data custom: dt_full/dt_date/dt_time agora priorizam DateTimeOriginal e usam os_date apenas como fallback.
[2.3.8.12.3] Expansao de normalizacao de metadados: PhotoMetadata recebeu novos aliases XMP/EXIF (AltitudeType, CaptureUUID, UTCAtExposure, PictureQuality, SensorFPS, LensPosition, LRFStatus, LRFTargetAbsAlt, GpsStatus e correlatos) e mapeamento dinamico por sufixo de normalized.
[2.3.8.12.4] ExifUtil passou a expandir GPSInfo em chaves individuais (GPSLatitude, GPSLongitude, GPSMapDatum, GPSStatus etc.) para aumentar cobertura dos REQUIRED_FIELDS.
[2.3.8.12.5] Validacao sintatica executada via py_compile para PhotoMetadata.py, PhotoMetadataTask.py e ExifUtil.py.
2.3.8.13 2026/04/07
[2.3.8.13] PhotoMetadataStep atualizado para suporte a tipagem String/Double: campos novos agora sao criados com inferencia por conteudo (numerico => QVariant.Double, textual => QVariant.String), removendo padrao anterior de tudo String.
[2.3.8.13.1] Normalizacao de valor agora respeita tipo do campo no layer (Double recebe float real; String preserva texto), mantendo datas como string e evitando tipos Date/DateTime.
[2.3.8.13.2] Inferencia ignora vazios/None para nao desclassificar campos numericos por causa de linhas em branco.
[2.3.8.13.3] Validacao sintatica executada via py_compile para core/engine_tasks/PhotoMetadataStep.py.
2.3.8.14 2026/04/07
[2.3.8.14] PhotoMetadataTask agora recebe source_points do ExecutionContext (pontos originais do MRK) e usa essa fonte para completar atributos ausentes do layer antes do enrich, reduzindo null em lat/lon/mrk_* quando a camada nao materializa todos os campos.
[2.3.8.14.1] PhotoMetadata.enrich ganhou opcao return_report=True e passou a expor json_dump_path/matched/not_found sem quebrar compatibilidade do retorno legado.
[2.3.8.14.2] Novo log explicito no PhotoMetadataTask com code=PHOTO_METADATA_JSON_PATH para mostrar o caminho do JSON temporario no mesmo contexto da task.
[2.3.8.14.3] PhotoMetadataStep passou source_points=context.get('points') para a task.
[2.3.8.14.4] Validacao sintatica executada via py_compile para PhotoMetadata.py, PhotoMetadataTask.py e PhotoMetadataStep.py.
2.3.8.15 2026/04/08
[2.3.8.15] Refatoracao estrutural de metacampos: criado core/model/Field.py com dataclass Field(normalized, core, label, attribute, description, level).
[2.3.8.15.1] MetadataFields.py migrado para dict de objetos Field em REQUIRED_FIELDS/CUSTOM_FIELDS/MRK_FIELDS, com level=3 para REQUIRED e level=5 para CUSTOM/MRK.
[2.3.8.15.2] LIGHT_SOURCE_VALUES removido de MetadataFields e convertido para enum em core/enum/LightSourceEnum.py (com get_label/get_description).
[2.3.8.15.3] Sistema adaptado ao novo formato: StringAdapter (dataclass-aware), PhotoMetadata (normalized via atributo), OBSOLETManager (normalized via atributo), CustomPhotosFieldsUtil (label de luz via LightSourceEnum).
[2.3.8.15.4] core/enum/__init__.py atualizado para exportar LightSourceEnum.
[2.3.8.15.5] Validacao sintatica executada via py_compile para modulos alterados.
2.3.8.16 2026/04/08
[2.3.8.16] Centralizacao reforcada no MetadataFields: adicionados resolvedores e utilitarios (all_fields, key_to_attribute_map, attribute_to_key_map, resolve_key, resolve_output_name, resolve_output_names, normalize_selected_keys, map_record_to_output_attributes, default_track_attribute_keys).
[2.3.8.16.1] Fluxo de aplicacao alinhado para usar MetadataFields como fonte de verdade de nomes de saida (attribute): PhotoMetadataTask, PhotoMetadataStep, DroneCoordinatesRunner e plugin DroneCoordinates.
[2.3.8.16.2] Selecao de campos em preferencias/UI passou por normalizacao canonica (aceitando entrada antiga e convertendo para chave valida do metadata).
2.3.8.17 2026/04/08
[2.3.8.17] Refatoracao de responsabilidade: VectorLayerGeometry.create_point_layer_from_dicts deixou de ser especifico de MRK e passou a receber schema do chamador (field_specs + geometry_keys), reduzindo acoplamento de dominio.
[2.3.8.17.1] MrkParseStep e MrkParser.to_point_layer agora montam e injetam schema MRK explicitamente no caller.
2.3.8.18 2026/04/08
[2.3.8.18] Dump JSON temporario de fotos reformatado para raw_records no formato por arquivo (\"<ARQUIVO>.JPG\": {...}).
[2.3.8.18.1] Dump passou a aplicar whitelist catalogada no MetadataFields (REQUIRED + CUSTOM + MRK), mantendo campos ausentes como null.
[2.3.8.18.2] Correcao de preenchimento MRK no dump: merge por sequencia da foto (DJI ... _0001_ ...) com contexto dos pontos MRK do grupo, evitando null indevido em foto/lat/lon/alt/mrk_*.
2.3.8.19 2026/04/08
[2.3.8.19] Padronizacao de nomenclatura no MetadataFields: chaves e normalized migrados para formato sem snake_case (PascalCase), incluindo exemplos como Path e SizeMb.
2.3.8.20 2026/04/09
[2.3.8.20] MetadataFields: Criado enum MetadataFieldKey em core/enum com todas as chaves dos dicionarios REQUIRED_FIELDS, CUSTOM_FIELDS e MRK_FIELDS. Substituido as chaves dos dicionarios por objetos enum. Ajustados metodos all_fields(), required_keys(), etc., para retornar strings conforme esperado pelo codigo legado. Adicionado import no __init__.py de core/enum.
[2.3.8.19.1] Compatibilidade retroativa mantida via resolve_key com conversao automatica de nomes antigos (snake_case -> PascalCase) para evitar quebra de fluxos legados.
[2.3.8.19.2] Ajuste de padrao de atributos: todos os attribute validados com limite maximo de 9 caracteres e descriptions sincronizadas com o codigo entre colchetes.

2.3.8.20 2026/04/08
[2.3.8.20] Inicio da implementacao do sistema de report (E03): ExplorerUtils ganhou infraestrutura de diretorios temporarios dedicados (`%TEMP%/cadmus/reports/json` e `%TEMP%/cadmus/reports/html`) com helpers de criacao e resolucao de caminhos.
[2.3.8.20.1] ExplorerUtils.create_temp_json foi expandido com `subfolder` e `file_stem_hint`, mantendo retrocompatibilidade e permitindo nomes de JSON mais amigaveis ao usuario.
[2.3.8.20.2] Adicionados helpers de naming para report (`build_report_json_stem`, `build_report_html_stem`, `build_temp_report_html_path`) preparando a etapa de geracao HTML.
[2.3.8.20.3] PhotoMetadata passou a salvar dump JSON no diretorio temporario dedicado de reports/json com naming sugestivo baseado em pasta de entrada e total de pontos.
[2.3.8.20.4] StringManager recebeu `_normalize_key` centralizado para normalizacao consistente de chaves (snake_case), conforme requisito do plano.
[2.3.8.20.5] Todo atualizado: etapa E03 marcada como concluida e item E05.2 concluido.
2.3.8.21 2026/04/08
[2.3.8.21] Etapa E04 concluida: sistema de report migrado para estrutura oficial do plugin com novos modulos em `utils/report` (JSONUtil, RangeMetadataManager, IMGMetadata, AggregateAnalyzer, RenderEngine) e recursos em `resources/reports` (template.html, config.yaml).
[2.3.8.21.1] Criado `core/services/ReportGenerationService.py` para orquestracao de geracao de relatorio HTML a partir de JSON temporario, usando pipeline JSONUtil -> IMGMetadata -> AggregateAnalyzer -> RenderEngine.
[2.3.8.21.2] Integracao inicial no DroneCoordinates (E06 parcial): quando `generate_report` esta marcado, o plugin consome `photo_metadata_json_path` do contexto e dispara a geracao de HTML sem interromper o fluxo vetorial em caso de erro.
[2.3.8.21.3] Integracao inicial no DroneCoordinatesRunner (E07 parcial): runner passou a ler `generate_report` nas preferencias e acionar ReportGenerationService ao final, retornando `report_payload` no callback `on_finished`.
[2.3.8.21.4] PhotoMetadataStep passou a propagar `json_dump_path` para o ExecutionContext (`photo_metadata_json_path`) para uso direto pela camada de servico de report.
[2.3.8.21.5] Validacao sintatica executada com sucesso via `py_compile` para todos os modulos alterados nesta rodada.
2.3.8.22 2026/04/08
[2.3.8.22] Etapa E08 concluida: criada nova ferramenta `Report Metadata` (categoria Agriculture, tipo Dialog) com plugin dedicado `plugins/ReportMetadataPlugin.py` para listar JSONs temporarios e regerar HTML sob demanda.
[2.3.8.22.1] ToolRegistry atualizado com novo item de menu/toolbar e executor `run_report_metadata`; ToolKey novo adicionado em `utils/ToolKeys.py` (`REPORT_METADATA`).
[2.3.8.22.2] i18n expandido (pt_BR/en/es/de) com strings da nova ferramenta: titulo, tooltip, seletor JSON, atualizar lista, gerar relatorio e abrir pasta de relatorios.
[2.3.8.22.3] WidgetFactory recebeu helper `create_open_reports_button(...)`, integrado ao plugin de report para navegacao rapida da pasta de HTMLs gerados.
[2.3.8.22.4] Integracao de report no fluxo principal refinada: DroneCoordinates e DroneCoordinatesRunner usam `ReportGenerationService`, com contexto `photo_metadata_json_path` vindo do PhotoMetadataStep.
[2.3.8.22.5] Validacao sintatica executada com sucesso via `py_compile` para todos os arquivos alterados nesta rodada.
2.3.8.23 2026/04/09
[2.3.8.23] Finalizacao da fase de implementacao do plano: consolidada centralizacao de metadata para report com `MetadataFields.resolve_candidates`, garantindo lookup consistente por key/attribute/prefixos (EXIF/XMP/MRK) em consumidores de analytics.
[2.3.8.23.1] Correcao de runtime em `IMGMetadata` (import `re`) para derivacao de `flight_id` e compatibilidade completa com analise agregada.
[2.3.8.23.2] Corrigida hierarquia de subpastas temporarias em `ExplorerUtils.ensure_temp_subfolder` (suporte real a `reports/json` e `reports/html` em vez de pasta unica sanitizada).
[2.3.8.23.3] `ReportMetadataPlugin` expandido com fluxo "fotos sem MRK": selecao de pasta, opcao recursiva, opcao de gerar report e acao de vetorizacao usando `PhotoFolderVectorizationService`.
[2.3.8.23.4] Novo fluxo gera camada vetorial (quando houver coordenadas), JSON de diagnostico em `%TEMP%/cadmus/reports/json` e HTML opcional em `%TEMP%/cadmus/reports/html`, com feedback na UI e atualizacao da lista de JSON.
[2.3.8.23.5] i18n atualizado (pt_BR/en/es/de) para os novos controles de report metadata (pasta JSON, bloco sem MRK, opcoes e mensagens de fallback).
[2.3.8.23.6] Documentacao adicionada: `docs/ia/GUIA_REPORT_METADATA_RAPIDO.md` e `docs/ia/CHECKLIST_REMOCAO_PHOTO_REPORT.md`.
[2.3.8.23.7] Validacao sintatica executada com sucesso via `py_compile` para modulos alterados desta rodada.
2.3.8.24 2026/04/09
[2.3.8.24] Correcao critica: Refactoring de enum MetadataFieldKey para evitar erros de comparacao nativa em operacoes sorted() e set(). PROBLEMA: PhotoMetadata.py tentava comparar diretamente enums (MetadataFieldKey objects) em sorted(), causando "'<' not supported between instances of 'MetadataFieldKey' and 'MetadataFieldKey'" durante PhotoMetadataTask. SOLUCAO: Padronizado uso de `.value` para converter enum -> string em todas as operacoes de iteracao/comparacao, garantindo que dictionaries, sets e sorted() trabalhem com strings, nao enums.
[2.3.8.24.1] PhotoMetadata.py: Alteradas linhas 54-56 para retornar strings `[k.value for k in MetadataFields.REQUIRED_FIELDS.keys()]` em lugar de enum keys. Mesmo padrao aplicado nas linhas 92 e 115 para MRK_FIELDS.
[2.3.8.24.2] PhotoMetadata.py linha 184: Alterado set comprehension de `set(MetadataFields.all_fields().keys())` para `set(k.value for k in MetadataFields.all_fields().keys())` para garantir operacoes sobre strings.
[2.3.8.24.3] PhotoMetadata.py linhas 391 e 431: Adicionado `key=lambda x: x.value if hasattr(x, 'value') else str(x)` ao sorted() para permitir comparacao transitoria de enums antes de conversao.
[2.3.8.24.4] PhotoFolderVectorizationService.py linha 64-67: Adicionado `key_str = key.value if hasattr(key, 'value') else str(key)` e uso em `resolve_output_name(key_str)` e `canonical.get(key_str)` para garantir operacoes com strings.
[2.3.8.24.5] IMGMetadata.py linha 22: Alterado `all_keys = list(MetadataFields.all_fields().keys())` para `all_keys = [k.value if hasattr(k, 'value') else str(k) for k in MetadataFields.all_fields().keys()]` para inicializar dicionario com chaves string.
[2.3.8.24.6] Validacao sintatica executada com sucesso via `py_compile` para PhotoMetadata.py, PhotoFolderVectorizationService.py e IMGMetadata.py.
[2.3.8.24.7] Padroes de conversao enum->string consolidados: Use `[k.value for k in ...]` para iteracoes; use `key.value if hasattr(key, 'value') else str(key)` quando transicoes sao necessarias; ensure dictionarios/sets recebem sempre strings, nunca enums.
2.3.8.25 2026/04/09
[2.3.8.25] Correcao critica: CheckboxGridWidget nao estava convertendo chaves enum para strings corretamente. PROBLEMA: Quando MetadataFields.REQUIRED_FIELDS tinha chaves enum, StringAdapter.to_key_label_description passava enums como "key", CheckboxGridWidget fazia str(enum) resultando em "MetadataFieldKey.FILE" em vez de "File", causando falha na normalizacao dos campos selecionados. SOLUCAO: CheckboxGridWidget._normalize_options agora usa `key.value if hasattr(key, 'value') else str(key)` para extrair o valor string dos enums, garantindo que get_checked_keys() retorne strings validas como "File" em vez de "MetadataFieldKey.FILE".
[2.3.8.25.1] Aplicado para ambos os formatos aceitos: dicionario (caso MetadataFields) e lista (caso StringAdapter).
[2.3.8.25.2] Validacao sintatica executada via py_compile para CheckboxGridWidget.py.
2.3.8.26 2026/04/09
[2.3.8.26] Correcao critica: bug em PhotoMetadata._build_selected_keys() tentando chamar .value em strings. PROBLEMA: known_keys = set(k.value for k in MetadataFields.all_fields().keys()), mas all_fields().keys() ja retorna strings (valores .value dos enums), causando "'str' object has no attribute 'value'". SOLUCAO: Alterado para known_keys = set(MetadataFields.all_fields().keys()), pois as chaves ja sao strings.
[2.3.8.26.1] Validacao sintatica executada via py_compile para PhotoMetadata.py.

2.3.8.27 2026/04/09
[2.3.8.27] Correcao critica: erro de comparacao entre MetadataFieldKey enum e str em PhotoMetadataTask. PROBLEMA: map_record_to_output_attributes recebia dicionario com chaves enum de PhotoMetadata.enrich, causando "'<' not supported between instances of 'MetadataFieldKey' and 'str'" ao tentar ordenar field_names. SOLUCAO: Conversao de chaves enum para string usando {k.value if hasattr(k, 'value') else str(k): v for k, v in item.items()} antes de passar para map_record_to_output_attributes.

2.3.8.28 2026/04/09
[2.3.8.28] Correcao de atributos duplicados em MetadataFields.py. PROBLEMA: FlightNum e FlightNam apareciam 2x (uma em CUSTOM_FIELDS derivado do MRK, outra em MRK_FIELDS original). Isso violava unicidade de atributos para shapefiles. SOLUCAO: Renomeou CUSTOM_FIELDS FlightNumber para DrvFNum (Derived Flight Number) e FlightName para DrvFNam (Derived Flight Name), preservando MRK_FIELDS originais com FlightNum e FlightNam. Total atributos unicos: 118, compatibilidade DBF (max 9 caracteres).
[2.3.8.28.1] Validacao sintatica executada com sucesso em MetadataFields.py via py_compile.

2.3.8.29 2026/04/09
[2.3.8.29] Governanca centralizada de metadados: aplicacao da "LAI (Lei de Atributos Ilegalizados)" em XmpUtil, ExifUtil e PhotoMetadata. PROBLEMA: 84 campos ilegais sendo gerados (arquivo, caminho, tamanho_mb, data_criacao, xmp:*, drone-dji:*, etc.) sem validacao contra MetadataFields, corrompendo schema de shapefiles. SOLUCAO: Tres camadas de sanitizacao implementadas:
[2.3.8.29.1] MetadataFieldKey.py: Adicionados 5 novos enums de EXIF faltantes (EXIF_SHARPNESS, EXIF_CONTRAST, EXIF_SATURATION, EXIF_FLASH_PIX_VERSION, EXIF_COLOR_SPACE).
[2.3.8.29.2] MetadataFields.py: Adicionadas definicoes de Field para os 5 novos campos EXIF em REQUIRED_FIELDS (nivel 3). Novo metodo classmethod sanitize_field_name(raw_field_name) que mapeia nomes brutos/ilegais para nomes canonicos em MetadataFields. Novo metodo is_authorized_field(field_name) para validar se campo e legalizaado.
[2.3.8.29.3] XmpUtil.py: Refatoracao completa para garantir apenas campos autorizados. _extract_file_metadata() retorna nomes canonicos (File, Path, SizeMb, DateTime em vez de arquivo, caminho, tamanho_mb, data_criacao). Novo metodo _sanitize_metadata() filtra output XMP contra MetadataFields.sanitize_field_name(). extract_metadata() aplica sanitizacao automatica antes de retornar, descartando campos nao autorizados com log DEBUG.
[2.3.8.29.4] ExifUtil.py: Refatoracao para compatibilidade com XmpUtil. extract_metadata_os() retorna nomes canonicos (File, Path, SizeMb, DateTime, DPIWidth, DPIHeight). extract_metadata_image() retorna nomes canonicos (ExifImageWidth, ExifImageHeight, Format, DPIWidth, DPIHeight). Novo extract_metadata_exif() sanitiza campos EXIF contra MetadataFields, rejeitando campo nao autorizado sob log DEBUG.
[2.3.8.29.5] PhotoMetadata.py: Ajuste de fallback dt_* para usar "DateTime" (canonizado) em vez de "os_date" (antigo).
[2.3.8.29.6] Impacto: 194 campos shapefile reduzidos para ~118+ campos auotrizados. Todas as 84 violacoes sao escaladas para rejecao silenciosa com auditoria em DEBUG. Resultado esperado: schemas consistentes, sem garbage fields, compatibilidade maxima com DBF (9 char limit).
[2.3.8.29.7] Validacao sintatica executada com sucesso via py_compile para MetadataFieldKey.py, MetadataFields.py, XmpUtil.py, ExifUtil.py e PhotoMetadata.py.
[2.3.8.29.8] PhotoFolderVectorizationService: filtro "sem MRK" aplicado no registro de saida e no JSON de raw_records. PROBLEMA: o modo sem MRK ainda incluia campos MRK no catalogo final, gerando lixo e nulos desnecessarios. SOLUCAO: removidos campos de MetadataFields.MRK_FIELDS do output_record e dos raw_records antes de gerar camada e relatorio.

[2.3.8.33] Created comprehensive expert documentation for the full Report system in docs/skills/report_system.md...

[2.3.8.34] Created comprehensive expert documentation for the full Processing Provider system (MTLProvider) in docs/skills/processing_provider.md, analyzing ALL 6 algorithms (RasterMassSampler/Clipper, DifferenceFields, AttributeStatistics, GeometryLineFromPoints, RasterDifferenceStatiscs) + BaseProcessingAlgorithm. Includes architecture (Mermaid), per-algo params/logic/code key, usage/customization/troubleshooting. Self-contained specialist guide.


#2.1.3.9.6 2026/03/22
- Transferida responsabilidade de criação de QAction da classe Tool para MenuManager: Tool.__init__ não cria mais self.action, MenuManager._create_actions_for_tools() cria QActions para todas as ferramentas e conecta executores; adicionado atributo tooltip à Tool para acesso no MenuManager.

#2.1.3.9.5 2026/03/22
- Corrigida ordem sequencial dentro de cada categoria no ToolRegistry: VECTOR (10,20,30,40,50), SYSTEM (10,20,30,40); movido load_folder para categoria VECTOR; ajustada ordem dos menus no MenuManager para SYSTEM(1), LAYOUTS(2), FOLDER(3), VECTOR(4), AGRICULTURE(5), RASTER(6); definido show_in_toolbar=True para todas as ferramentas; order agora representa a posição dentro do menu da categoria correspondente.

#2.1.3.9.4 2026/03/22
- Reorganizado CadmusPlugin.initGui(): seção "4) CONFIGURAÇÃO DAS AÇÕES" agora apresenta ações ordenadas sequencialmente por (10, 20, 30...130), agrupadas por categoria com comentários visuais (LAYOUTS, SYSTEM, VECTOR, AGRICULTURE); cada ação comentada com seu número de ordem correspondente à ToolRegistry para consistência e rastreabilidade; mantido alinhamento com Tool.order e MenuManager sorting.

#2.1.3.9 2026/03/21
- Nova arquitetura criada: Tool (resources/widgets/Tool.py), ToolRegistry (core/ToolRegistry.py) e MenuManager (core/MenuManager.py); DropdownToolButton atualizado para suportar separator (None) em secondary_actions.

#2.1.3.8 2026/03/21
- Refatoração crítica do DropdownToolButton: substituído QToolButton por QAction com setMenu, conectando triggered à main_action para comportamento correto de dropdown (clique no botão executa ação principal, seta abre menu); CadmusToolBar alterado para addAction(button.action).

#2.1.3.6 2026/03/21
- Correção no CadmusToolBar: alterado add_dropdown_buttons para adicionar QToolButton diretamente via addWidget(button.button) em vez de usar QWidgetAction, resolvendo erro de tipo 'DropdownToolButton' não sendo QWidget.

#2.1.3.5 2026/03/21
- Correção crítica no DropdownToolButton: removida herança de QWidgetAction (inapropriada para toolbars) e alterada para classe simples que configura QToolButton diretamente, resolvendo problema de cliques não funcionais na toolbar.

#2.1.3.4 2026/03/21
- Correção na funcionalidade dos botões dropdown: adicionado clicked.connect no DropdownToolButton.setup() para garantir que cliques no botão acionem a ação principal; adicionado logging de debug no CadmusPlugin.initGui() para rastrear criação de botões e facilitar diagnóstico.

#2.1.3.3 2026/03/21
- Refatoração adicional: criado widget CadmusToolBar exclusivo em resources/widgets/CadmusToolBar.py para gerenciar toolbar com método add_dropdown_buttons em massa; modificado DropdownToolButton para herdar de QWidgetAction, encapsulando QToolButton interno; reduzido código repetido na criação de botões na CadmusPlugin, mantendo referências individuais para limpeza.

#2.1.3.2 2026/03/21
- Refatoração adicional da classe CadmusPlugin: extraído método _add_toolbar_dropdown para widget exclusivo DropdownToolButton em resources/widgets/DropdownToolButton.py, reduzindo responsabilidades da classe principal e melhorando reutilização.

#2.1.3.1 2026/03/21
- Refatoração da classe CadmusPlugin para aliviar responsabilidades: criada classe PluginBootstrap em core/PluginBootstrap.py, responsável pela inicialização de logs, handler global de erros, limpeza de logs e registro do provider de processamento. CadmusPlugin agora orquestra via PluginBootstrap.

#2.1.3 2026/03/20
- Internacionalização expandida para os plugins `SettingsPlugin`, `ReplaceInLayouts`, `LoadFolderLayers`, `GenerateTrailPlugin`, `CopyAttributesPlugin`, `ExportAllLayouts` e `coord_result_dialog`, com centralização das strings exibidas ao usuário em `Strings_pt_BR`.
- Reorganização do catálogo `Strings_pt_BR` em grupos mais intuitivos, separando botões, labels comuns, mensagens comuns e títulos específicos de plugins para incentivar reaproveitamento de strings genéricas.
- Traduções completas adicionadas em `Strings_en` e `Strings_es` para todas as novas chaves introduzidas no catálogo em português.
- Atualização do histórico para consolidar as mudanças recentes de internacionalização e padronização dos plugins.

#2.1.2.1 2026/03/20
- Refatorado DroneCoordinates para usar strings internacionalizadas via STR e Strings_pt_BR, substituindo todas as strings hardcoded exibidas ao usuário.

#2.1.2 2026/03/20

#2.1.3.9.3 2026/03/22
- Reorganizado ToolRegistry._create_tool_list() com comentários separando por categorias (LAYOUTS, SYSTEM, VECTOR, AGRICULTURE) mostrando a ordem (order) de cada ferramenta.
- Atualizado tooltips completas em ToolRegistry copiadas de cadmus_plugin.py, com todas as informações detalhadas.
- Melhorada legibilidade do código com separação visual por seção de categoria.

- Adicionado `order` (int, padrão 100) ao Tool para ordenação de menus e toolbar.
- Adicionado `show_in_toolbar` (bool, padrão True) ao Tool para controle fino sobre visibilidade em toolbar.
- Removido `executor_name` de todas as ferramentas (redundante com `executor`).
- MenuManager.create_toolbar() agora respeita `show_in_toolbar` e ordena ações secundárias por `order`.
- MenuManager.populate_menus() ordena ferramentas por `order` dentro de cada categoria.

- Corrigido ToolRegistry para importar MenuManager e remover bloco duplicado residual após get_tools(), eliminando erro de sintaxe.
- Adaptado ToolRegistry para aceitar iface no __init__, mover todos os run_ methods para ToolRegistry, corrigir imports para ..plugins, adicionar executor=self.run_xxx em cada Tool para conectar ações diretamente.
- Unificado constantes de categoria em MenuManager para uppercase, consistente com ToolRegistry.
- ToolRegistry agora centraliza criação de ferramentas e execução, reduzindo responsabilidades do CadmusPlugin.


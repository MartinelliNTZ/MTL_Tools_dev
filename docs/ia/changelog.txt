
changelog=
1.6.8 2026/03/09
      1.6.8.01
      - PADRONIZAÇÃO: `load_folder_layers` UI com BasePluginMTL + WidgetFactory
        - Substituída UI manual por `_build_ui()` padronizada usando:
          - `WidgetFactory.create_path_selector` para seleção de pasta (modo folder)
          - `WidgetFactory.create_collapsible_parameters` + `WidgetFactory.create_checkbox_grid` (2 colunas) para tipos de arquivo
          - `WidgetFactory.create_checkbox_grid` (1 coluna) para opções: carregar apenas não carregados, preservar estrutura, criar backup
          - `WidgetFactory.create_bottom_action_buttons` para botões padrão (Carregar / Fechar / Info)
        - Preferências (`_load_prefs` / `_save_prefs`) adaptadas para usar API dos widgets
        - Lógica de execução (`run_tool`) preservada; comportamento inalterado
        - Risco: Baixíssimo (apenas padronização da camada UI)
        ✓ Apenas adiciona persistência, sem lógica adicional

      1.6.8.02
      - LOG / PREFS / GPKG / EXPLORER
        - Adicionado: logging em `plugins/load_folder_layers.py` usando `self.logger` (BasePluginMTL)
        - Persistência: estado do painel "Tipos de Arquivo" (`types_expanded`) salvo/carregado nas preferências
        - Suporte: GeoPackage (`.gpkg`) adicionado ao `FILE_TYPES` e ao `ExplorerUtils.VECTOR_EXTS`
        - ExplorerUtils: `load_raster_from_file()` implementado como método estático wrapper (parâmetro nomeado `external_tool_key="untraceable"`)
        - ProjectUtils: `ensure_group()` usa `s = rel_path.split(os.sep)` para clareza ao criar grupos aninhados
        - Clean-up: remoção do override `show_info()` em `load_folder_layers` (usa `show_info_dialog()` da classe-base)
        - Risco: Baixo (muda apenas logging, prefs e delega carregamento de rasters para wrapper estático)

      1.6.8.03
      - ESTATÍSTICAS: métricas por arquivos/feições/tamanho
        - `start_stats()` agora aceita `total_files` e escolhe automaticamente entre métricas por arquivos, por feições ou por bytes
        - `finish_stats()` grava agregados e calcula `avg_speed_files`, `avg_speed_features` e `avg_speed` (bytes/s)
        - Mensagens resumidas exibidas via `QgisMessageUtil`; preferências atualizadas e salvas
        - Risco: Baixo (compatível com chaves de prefs anteriores)

      1.6.8.04
      - BUG FIX: Camadas não apareciam e QGIS travava após carregar pasta
        - Remove bloqueio de sinais da árvore de camadas em `LoadFilesStep`.
        - As camadas passam a aparecer imediatamente sem exigir ação do usuário.
        - Evita crashes ao interagir com a árvore de camadas durante carregamento.

      1.6.8.05
      - BUG FIX: "Carregar apenas arquivos ainda NÃO carregados" (modo assíncrono)
        - `missing_only` agora filtra corretamente registros antes de adicionar ao projeto.
        - Evita recarregar camadas já presentes no projeto (reduz memória/tempo).

      1.6.8.06
      - FEATURE: opção "Não agrupar a última pasta" (modo preserve)
        - Ao ativar, o plugin não cria grupo para a última pasta que contém o arquivo.
        - Checkbox é automaticamente desabilitado quando "Preservar estrutura" está desligado.



      1.6.8.07
      - PADRONIZAÇÃO: tamanho padrão para componentes UI
        - Adicionado `STANDARD_SIZE` em `Styles.py` e usado para input/button/checkbox/radio.
        - Substituídos hardcoded `12px` e `min-height: 12px` por constantes (`Styles.INPUT_HEIGHT`, `Styles.BUTTON_HEIGHT`, `Styles.CHECKBOX_SIZE`).
        - Removido código morto em `Styles.label()` (retorno duplicado).


      1.6.8.08
      - ESTILOS: centralização de botões/inputs e remoção de cores hardcode
        - Criado Styles.base_button(), Styles.base_input(), Styles.base_checkbox()
        - Refatorado Styles.file_selector(), Styles.attribute_selector(), Styles.path_selector_widget(), Styles.bottom_action_buttons_widget() para usar estilos base
        - Unificado Styles.simple_button_widget() para usar Styles.base_button() e removida definição duplicada
        - Eliminado `#ffffff` hardcoded em botões; agora usa `Styles.COLOR_BUTTON_TEXT`
        - Removidos blocos hover/pressed/disabled repetidos em múltiplos estilos

        1.6.8.09
        - BUG FIX: Compatibilidade Python 3.7 (QGIS 3.16)
          - Corrigido uso de anotações que utilizavam tipos embutidos subscriptáveis (ex.: `list[QCheckBox]`) que causavam o erro `TypeError: 'type' object is not subscriptable` em ambientes Python <3.9.
          - Arquivo modificado: resources/widgets/CheckboxGridWidget.py — substituído `list[QCheckBox]` por `List[QCheckBox]` e adicionado `from typing import List`.
          - Risco: Muito Baixo
            - Mudança localizada no widget; funcionalidade preservada.

  1.6.8.10
  - FEATURE: ReadOnlyFieldWidget (widget exclusivo)
    - Adicionado `resources/widgets/ReadOnlyFieldWidget.py` — widget para mostrar campos somente-leitura com botão de copiar individual e botão opcional "Copiar tudo".
    - Integração: `core/ui/WidgetFactory.create_readonly_field()` criada para facilitar uso em dialogs.
    - Parametros suportados: `title`, `fields` (dict com `title`, `value`, `value_type`, `titlebutton`), `num_columns`, `copy_all_button_title`.
    - Risco: Muito Baixo — widget novo e localizado, sem impacto em código existente até ser utilizado.


  1.6.8.11
  - UI: botão "Copiar Localização (Completo)" via WidgetFactory
    - Substituído o botão direto no diálogo por `WidgetFactory.create_simple_button()` em `plugins/coord_result_dialog.py`.
    - Implementado `CoordResultDialog.copy_all_info()` que agrega WGS, UTM, altitude e endereço e copia para a área de transferência usando `ProjectUtils.set_clipboard_text()`.
    - Removidos métodos auxiliares não usados: `_line()` e `_copy_button()` do diálogo.
    - Risco: Muito Baixo — alteração localizada na UI do diálogo; comportamento de cópia preservado e centralizado.

  1.6.8.12
  - PADRONIZAÇÃO: CoordResultDialog herda BasePluginMTL e usa logger
    - `plugins/coord_result_dialog.py` agora herda `BasePluginMTL` e chama `self.init(...)` para inicializar `self.logger` e preferências.
    - Adicionados logs (`self.logger.debug/info/warning`) em pontos chave: `update_info`, `set_altitude`, `set_address`, `copy_all_info`, `copy_address`.
    - Notificação ao usuário: `QgisMessageUtil.bar_success()` é chamada quando a cópia para a área de transferência ocorre com sucesso.
    - Persistência: restauração e persistência do último tamanho da janela agora reuse a implementação de `BasePlugin` (`_restore_window_size` / `_persist_window_size`).
    - Risco: Muito Baixo — padronização da construção do diálogo e adição de logging/feedback visual.

  1.6.8.13
  - CORREÇÕES: Robustez de diálogo de coordenadas e criação de MainLayout
    - BasePlugin._build_ui: garante criação incondicional de `MainLayout` (evita `None` em `self.layout._frame`), prevenindo AttributeError durante construção de diálogos.
    - CoordClickTool: protege a criação/mostra do `CoordResultDialog` e evita agendar tarefas assíncronas se o diálogo não foi criado corretamente (previne callbacks que acessam `self.dialog` = None).
    - CoordResultDialog:
      - Adicionado `_build_ui`, `_load_prefs` e `_save_prefs` seguindo padrão `GenerateTrailPlugin` para garantir ordem correta de criação de `self.layout` e widgets.
      - Moved UI composition to `_build_contents()` and removed direct duplicate construction in `__init__`.
      - Adicionado botão `Copiar Localização (Completo)` via `WidgetFactory.create_simple_button()` posicionado entre os labels de endereço e os botões inferiores; botão conectado a `copy_all_info()`.
      - Melhorias de logging e tratamento de erros em pontos críticos de atualização e callbacks.
    - Risco: Muito Baixo — correções de ordem de inicialização e proteção contra estados parcialmente construídos; comportamento do diálogo preservado.

  1.6.8.14
  - SESSÃO: Padronização, robustez e correções (resumo da sessão de desenvolvimento)
    - Compatibilidade e tipagens: finalizada a correção de anotações PEP585 (Python <3.9) em widgets críticos para evitar TypeError em QGIS 3.16.
    - Novo widget e Factory: `ReadOnlyFieldWidget` adicionado e integrado via `WidgetFactory.create_readonly_field()` para campos somente-leitura com botões de cópia individuais e opcional "copiar tudo".
    - Clipboard centralizado: `ProjectUtils.set_clipboard_text()` criado/uso consolidado para operações de copiar (endereço, localização completa).
    - `CoordResultDialog`:
      - Refatorado para herdar `BasePluginMTL` e usar `MainLayout` através de `_build_ui()` → `_build_contents()` (garante ordem correta de criação dos widgets).
      - Adicionados métodos `_load_prefs()` e `_save_prefs()` consistentes com outras ferramentas (persistência de tamanho/estado).
      - Botão `Copiar Localização (Completo)` adicionado via `WidgetFactory.create_simple_button()` e conectado a `copy_all_info()`.
      - Logs e tratamento de erros reforçados em `update_info`, `set_altitude`, `set_address`, `copy_all_info` e `copy_address`.
    - `BasePlugin`:
      - `_build_ui()` alterado para garantir criação incondicional de `MainLayout` (evita `None` em `self.layout._frame`).
    - `CoordClickTool`:
      - Protegida a criação/mostra do diálogo; evita agendar tarefas assíncronas quando o diálogo não foi criado corretamente (previne callbacks que acessam `self.dialog` = None).
      - Garante `raise_()`/`activateWindow()` ao mostrar o diálogo quando possível.
    - Ajustes menores: `GenerateTrailPlugin` e outros plugins verificados para seguir o padrão `_build_ui` + `self.layout.add_items()` (consistência arquitetural).
    - Risco: Muito Baixo — foco em ordem de inicialização, tratamento de estados parciais e padronização de UI/Prefs; mudanças localizadas e testáveis.

  1.6.7 2026/03/09
      1.6.7.10
    - REFATORAÇÃO: CheckboxGridWidget e uso de dicionário chave→label
      - WidgetFactory.create_checkbox_grid agora utiliza widget exclusivo CheckboxGridWidget
      - Interface alterada: recebe dicionário {chave: label} ao invés de lista
      - Plugins atualizados: export_all_layouts.py, DroneCoordinates.py, replace_in_layouts.py
      - Preferências e acessos agora usam as chaves, evitando bugs de duplicidade de label
      - Compatibilidade e integração mantidas
      - Risco: Baixíssimo (interface padronizada, plugins migrados)
    1.6.7.9
    - BUG FIX: PDFUtils logging crash (PNG merge silencioso)
      - Problema: "Unindo 3 PNGs em PDF" era logado, mas depois silêncio total (5s)
        ✓ Logs não aparecem: NENHUM log de PDFUtils.merge_pngs_to_pdf()
        ✓ Causa: LogUtilsNew.get_logger() não existe - deve instanciar diretamente
        ✓ Erro foi: `LogUtilsNew.get_logger()` → não é método estático
      
      1.6.8.10
      - FIX: restaurado padrão `(layout, widget)` em `create_simple_button`
        - Revertida mudança que retornava `QWidget` container para preservar
          a API consistente da `WidgetFactory` (sempre retorna container+widget)
        - Adicionado `layout.addSpacing(6)` antes do separador quando
          `separator_bottom=True` para evitar que o botão "encave" visualmente
          com itens abaixo (corrige perda parcial de altura observada)
        - Risco: Baixíssimo (ajuste de espaçamento; compatibilidade preservada)
      - Solução implementada:
        ✓ Alterar: LogUtilsNew.get_logger() → LogUtilsNew(...) instância
        ✓ Adicionar try/except robustuem _get_logger() com fallback SimpleLogger
        ✓ Adicionar try/except outer em merge_pdfs() e merge_pngs_to_pdf()
        ✓ Adicionar traceback completo em logger.error()
        ✓ Adicionar ImportError específica para PIL e PyPDF2
      
      - Impacto:
        ✓ PNG merge agora loga todas etapas (ou erros precisos)
        ✓ Fallback SimpleLogger se LogUtilsNew falhar
        ✓ Traceback completo para diagnóstico de erros
        ✓ Zero chance de crash silencioso
      
      - Risco: Muito Baixo
        ✓ Apenas correção de logging
        ✓ Lógica core idêntica

    1.6.7.8
    - REFATORAÇÃO: PDFUtils + DependenciesManager (arquitetura melhorada)
      - Problema identificado: PNG merge crashava silenciosamente (sem logs)
        ✓ Root cause: merge_pngs_to_pdf sem tratamento de erros/logging
      - Solução: Separação de responsabilidades e logging assertivo
        
      [1] Classe PDFUtils - Merge de PDFs/PNGs com logs detalhados
        ✓ merge_pdfs(): Mescla PDFs com validação e logging
        ✓ merge_pngs_to_pdf(): Mescla PNGs com redimensionamento e logging detalhado
        ✓ Logs em cada etapa: verificação deps, arquivo processado, redimensionamento, output
        ✓ Tratamento completo de exceções
        ✓ Usa LogUtilsNew para logging estruturado
      
      [2] Classe DependenciesManager - Gerencia dependências externas
        ✓ check_dependency(nome): Valida se módulo está instalado
        ✓ get_dependency_info(nome): Retorna info sobre dependência
        ✓ get_install_script_path(nome): Localiza script .cmd/.bat
        ✓ install_dependency(nome): Executa instalação via script
        ✓ validate_dependencies(lista): Valida múltiplas deps
      
      [3] Refatoração export_all_layouts.py
        ✓ Imports otimizados: PDFUtils, DependenciesManager
        ✓ Validação: merge_utils.HAS_PYPDF2 → DependenciesManager.check_dependency()
        ✓ Instalação: merge_utils.install_pypdf2() → DependenciesManager.install_dependency()
        ✓ Merge: merge_utils.merge_pdfs() → PDFUtils.merge_pdfs()
        ✓ Log detalhado de max_width na chamada de PNG merge
      
      [4] Backward compatibility em pdf_png_merge_utils.py
        ✓ Mantido como wrapper deprecated
        ✓ Redireciona para novas classes (sem breaking changes)
        ✓ Aviso DeprecationWarning para código legado
      
      - Impacto:
        ✓ Logs completos de todas operações de merge
        ✓ Melhor diagnóstico de falhas (onde e por quê)
        ✓ Arquitetura escalável para futuras dependências
        ✓ Zero breaking changes para código existente
      
      - Risco: Muito Baixo
        ✓ Apenas reorganização de código
        ✓ Lógica core idêntica à anterior
        ✓ Backward compatibility garantida

    1.6.7.7
    - AJUSTE VISUAL: CheckboxGrid altura de linha refinada
      - Problema: Checkbox 12px estava sendo cortada nas extremidades superior/inferior
      - Causa: Altura da linha = 12px (mesmo tamanho do checkbox), sem padding
      - Solução em create_checkbox_grid():
        ✓ chk.setFixedHeight(14) aumentado de 12px para 14px
        ✓ Fornece 1px de padding acima e 1px abaixo do checkbox
        ✓ v_spacing mantido em 2px entre items
      - Espaçamento preservado:
        ✓ addSpacing(6) após título
        ✓ addSpacing(6) antes de separador_bottom
      - Resultado: Checkbox totalmente visível, sem cortes
      - Impacto: Visual refinado, melhor usabilidade
      - Risco: Muito Baixo (apenas ajuste de altura)

    1.6.7.6
    - BUG FIX: ExportAllLayoutsDialog - PNG merge silencioso e checkbox grid spacing
      - Problema 1: "Unir PNGs em PDF final" não unia e não exibia sucesso
        ✓ Causa: merge_pngs_to_pdf() bem sucedido não mostrava mensagem final
        ✓ Lógica anterior: só exibia erro (if not ok), silenciava sucesso
        ✓ Solução: Adicionado flag merge_png_success para tracking
          - Se ok=True: log "✓ PNGs unidos com sucesso" + flag=True
          - Se ok=False: log warning + erro adicionado à lista
        ✓ Resultado: Exibição clara de sucesso/falha no diálogo final
      - Problema 2: Checkboxes no grid muito apertadas (falta espaço)
        ✓ Causa: Spacing vertical = 2px + sem espaço após título
        ✓ Solução em create_checkbox_grid():
          - Aumentado v_spacing de 2px para 6px entre items
          - Adicionado addSpacing(6) após título
          - Adicionado addSpacing(6) antes de separador_bottom
        ✓ Resultado: Grid visualmente mais arejado e legível
      - Melhorias:
        ✓ PDF merge também agora com flag merge_pdf_success
        ✓ Mensagem final mostra resumo: layouts exportados + merges bem-sucedidos
        ✓ Log detalhado com status de cada merge
      - Impacto: 
        ✓ Usuário vê claramente resultado de merge PNG/PDF
        ✓ UI mais confortável visualmente
      - Risco: Muito Baixo
        ✓ Apenas logging e spacing (visual)
        ✓ Lógica de merge intacta

    1.6.7.5
    - BUG FIX: ExportAllLayoutsDialog - erro de atributo QgsLayoutExporter e tamanho do botão
      - Problema 1: QgsLayoutExporter.PrinterResolution não existe
        ✓ Erro: "type object 'QgsLayoutExporter' has no attribute 'PrinterResolution'"
        ✓ Causado: ao refatorar para WidgetFactory, usei atributo inválido
        ✓ Solução: Voltar ao padrão original
          - PDF: `exporter.exportToPdf(path, QgsLayoutExporter.PdfExportSettings())`
          - PNG: `exporter.exportToImage(path, QgsLayoutExporter.ImageExportSettings())`
      - Problema 2: SimpleButtonWidget ("Usar pasta do projeto") muito grande
        ✓ Causa: setSizePolicy(Expanding, Expanding) ocupava altura total
        ✓ Solução: setSizePolicy(Expanding, Fixed) + setFixedHeight(Styles.BUTTON_HEIGHT)
        ✓ Resultado: Largura expande (preenche), altura fixa 12px
      - Impacto: ExportAllLayoutsDialog funciona corretamente
        ✓ Botão de pasta com tamanho correto
        ✓ Exportação de PDFs/PNGs normalizado
      - Risco: Muito Baixo
        ✓ Apenas correção de atributos/dimensões
        ✓ Lógica intacta

    1.6.7.4
    - REFATORAÇÃO: ExportAllLayoutsDialog padronização com BasePluginMTL + WidgetFactory
      - Antes: Herança de QDialog com UI manual (QCheckBox, QLineEdit, QPushButton espalhados)
        ✓ Código duplicado para load/save preferências
        ✓ Sem padronização visual com plugins
        ✓ Sem AppBar, sem MainLayout encapsulado
      - Depois: Herança de BasePluginMTL com WidgetFactory
        ✓ Checkboxes: 5 itens em grid 2 colunas via create_checkbox_grid()
        ✓ Max Width: InputFieldsWidget com um campo int via create_input_fields_widget()
        ✓ Pasta Saída: PathSelector (modo folder) via create_path_selector()
        ✓ Botão Projeto: SimpleButton com callback _set_folder_to_project()
        ✓ Botões Ação: create_bottom_action_buttons() (Exportar, Fechar, Info)
        ✓ AppBar integrada automaticamente via _build_ui()
        ✓ MainLayout sem scroll (enable_scroll=False, simples)
      - Persistência automática:
        ✓ _load_prefs() carrega checkboxes, max_width, pasta_saida, tamanho_janela
        ✓ _save_prefs() salva tudo automaticamente
        ✓ BasePlugin.closeEvent() chama _save_prefs() automaticamente
        ✓ Remove: carregar_preferencias(), salvar_preferencias(), métodos manual
      - Lógica de negócio mantida:
        ✓ execute_tool() exporta PDFs/PNGs com mesma lógica
        ✓ Validação de dependências (PyPDF2, Pillow) oferece instalação
        ✓ Unão de PDFs/PNGs funciona igual
        ✓ Modal final com resultado idêntico
      - Métodos novos:
        ✓ _set_folder_to_project(): callback do SimpleButton
        ✓ Antes: definir_pasta_projeto(), selecionar_pasta() - agora não precisa
      - Compatibilidade:
        ✓ Função run_export_all_layouts() continua igual
        ✓ TOOL_KEY.EXPORT_ALL_LAYOUTS continua mesmo
        ✓ Preferências migram automaticamente (load_tool_prefs usa mesma chave)
      - Benefícios:
        ✓ Interface padronizada com outros plugins (SettingsPlugin, GenerateTrailPlugin)
        ✓ 100+ linhas removidas (sem UI manual)
        ✓ Código mais limpo e manutenível
        ✓ Autocompletar AppBar, resize, tamanho janela persistido
        ✓ Logging automático via BasePluginMTL
      - Risco: Baixo
        ✓ Lógica de negócio intacta
        ✓ Preferências continuam funcionando
        ✓ Widgets WidgetFactory já testados em outros plugins
        ✓ Reversível se houver problema

    1.6.7.3
    - BUG FIX: Separador visual em create_double_spin_input ficava na mesma linha do widget
      - Problema: função usava QHBoxLayout (horizontal) para tudo
        ✓ Separador_top era adicionado horizontalmente à esquerda do label
        ✓ Separador_bottom era adicionado horizontalmente à direita do spinbox
        ✓ Layout permanecia na mesma linha: [separador] [label] [spin] [separador]
      - Solução: estrutura de layout aninhado
        ✓ QVBoxLayout externo (main_layout) → contém separadores em linhas distintas
        ✓ QHBoxLayout interno (h_layout) → contém apenas label + spin na mesma linha
        ✓ Retorno agora é (main_layout, spin) com separadores em posição correta
      - Impacto: Separadores visuais agora aparecem em linhas distintas (UI correta)
        ✓ SettingsPlugin (precision, threshold) agora renderizam corretamente
        ✓ GenerateTrailPlugin (tamanho implemento) também beneficiado
        ✓ Qualquer widget que use create_double_spin_input terá UI consistente
      - Risco: Muito Baixo
        ✓ Apenas reorganização de layout, sem mudança lógica
        ✓ Retorno continua sendo (layout, widget)
        ✓ Compatível com código existente em SettingsPlugin, GenerateTrailPlugin

    1.6.7.2
    - BUG FIX: SettingsPlugin crash com erro "wrapped C/C++ object of type QRadioButton has been deleted"
      - Problema: múltiplas chamadas a self.layout.add_items() causavam reparentings repetidos
        ✓ Cada chamada a add_items() reconstruía layouts, deletando widgets internos prematurely
        ✓ RadioButtonGridWidget perdia referência aos QRadioButton filhos
        ✓ _load_prefs() tentava acessar widget deletado → segmentation fault C++
      - Solução: consolidar em chamada ÚNICA a add_items()
        ✓ Acumular todos os items: prefs_label, pref_link, calc_layout, prec_layout, thresh_layout, buttons_layout
        ✓ Chamar self.layout.add_items([...lista completa...]) uma só vez ao final
        ✓ Evita reparentings repetidos e garante integridade de widget lifecycle
      - Impacto: SettingsPlugin agora funciona corretamente
        ✓ Preferences carregam e salvam sem crash
        ✓ RadioButtonGridWidget mantém referências válidas
        ✓ Layout é construído uma vez com estabilidade
      - Risco: Muito Baixo
        ✓ Apenas reorganization de chamadas existentes
        ✓ Zero lógica de negócio alterada
        ✓ Pattern agora alinhado com best practices Qt (minimal reparenting)
      - Aprendizado: MainLayout.add_items() espera execução ÚNICA
        ✓ Implementar de forma idêntica em outros plugins que o usem (GenerateTrailPlugin, etc)

    1.6.7.1
    - DOCUMENTAÇÃO: Análises de Cenário para GenerateTrailPlugin, VectorFieldsCalculationPlugin e ReplaceInLayouts
      - Criadas: 3 novos arquivos de análise de cenário seguindo padrão arquitetônico
        ✓ docs/analise_de_cenario/06_gerar_rastro_generalizado_analise_cenario.MD
          - Visão geral, objetivo, fluxo do GenerateTrailPlugin
          - Cenários de uso: memória, GeoPackage, assíncrono larga escala, seleção, CRS em graus
          - Tipos de geometria, impactos nos dados, riscos e controles
          - Observações sobre classes-chave: VectorLayerProjection, ExplodeStep, BufferStep, SaveVectorStep
          - Evoluções recomendadas: reprojeção automática, dissolução, feedback granular
        ✓ docs/analise_de_cenario/07_calcular_campos_vetoriais_analise_cenario.MD
          - Visão geral, objetivo, fluxo de VectorFieldsCalculationPlugin
          - Validações críticas: KML bloqueante, CRS geográfico automático, colisão de campos
          - Cenários de uso: polígonos métrico, WGS84 com fallback, larga escala, pontos com altimetria
          - Conflito de nomes, modo "Ambos", bloqueio KML, altimetria assíncrona
          - Observações: is_geographic_crs(), generate_field_name_with_suffix(), AltimetriaTask thread-safety
          - Evoluções: seleção de subset, reprojeção transparente, cache SRTM, progresso granular
        ✓ docs/analise_de_cenario/10_replace_in_layouts_analise_cenario.MD
          - Visão geral, objetivo, fluxo de ReplaceInLayoutsDialog
          - Backup automático timestamped + log detalhado em /backup/
          - Cenários: atualização simples, case sensitive, modo chave, sem backup, zero resultados
          - Múltiplos layouts, caracteres especiais, botão swap, troca rápida
          - Tipos de layout, impactos, riscos: texto em branco, sobrescrita desejada, sem backup
          - Observações: InputFieldsWidget, LayoutsUtils, ProjectUtils, LogUtilsNew
          - Evoluções: preview pré-execução, undo integrado, regex, filtro layouts, CSV batch
      - Atualizada: resources/instructions/replace_in_layouts_help.MD
        ✓ Título traduzido: "Replace Text in Layouts" → "Substituir Texto em Layouts"
        ✓ Adicionada seção detalhada de backup com exemplos de paths e estrutura de log
        ✓ Documentação de ⇅ Botão Inverter com casos de uso
        ✓ Nova seção "Desfazendo uma Substituição" com 3 opções práticas:
          - Opção 1: Botão Inverter (rápido, undo manual)
          - Opção 2: Restauração de backup (arquivo anterior)
          - Opção 3: Edição manual em último caso
        ✓ Adicionada seção "Preferências e Memória de Estado":
          - Texto buscado/substituição lembrados
          - Estado de checkboxes persistido
          - Tamanho de janela restaurado
          - Local de armazenamento documentado (~/.qgis3/)
        ✓ Aprimorada seção "Boas práticas" com avisos alarmantes
        ✓ Exemplo detalhado de arquivo de log com formatação real
      - Benefícios:
        ✓ Documentação arquitetônica completa para manutenção futura
        ✓ Cenários reais mapeados (ajuda na detecção de bugs)
        ✓ Help de usuário expandido e mais intuitivo
        ✓ Referências cruzadas entre análises facilitam compreensão sistêmica
      - Risco: Muito Baixo
        ✓ Documentação pura, zero impacto em código executável
        ✓ Padrão seguido (compatível com análises existentes v1.6.5, 1.6.6)
    1.6.7.1# COMEÇA AQUI



  1.6.6 2026/03/09
    1.6.6.4
    - PERSISTÊNCIA: Tamanho de Janela em ReplaceInLayouts
      - Adicionado: window_width e window_height em _save_prefs()
        ✓ Arquivo: plugins/replace_in_layouts.py (_save_prefs method)
        ✓ Padrão: Consistente com DroneCoordinates e outros plugins
      - Benefício: ReplaceInLayouts agora abre com tamanho anterior
      - Implementação:
        ✓ Linha ~109: "window_width": self.width()
        ✓ Linha ~110: "window_height": self.height()
      - Funcionamento: BasePlugin.closeEvent() chama _save_prefs() automaticamente
      - Risco: Baixo
    1.6.6.3
    - FEATURE: Botão de Troca (Swap) em ReplaceInLayouts
      - Novo: Botão "⇅ Inverter" entre campos old_text/new_text
        ✓ Inverte o conteúdo dos campos ao clicar
        ✓ old_text ↔ new_text (troca de posição)
      - Implementação:
        ✓ Arquivo: plugins/replace_in_layouts.py
        ✓ Método: _swap_fields() - inverte valores dos campos
        ✓ Widget: SimpleButtonWidget via WidgetFactory.create_simple_button()
      - Layout:
        ✓ Posição: entre input_fields_widget e checkbox_grid
        ✓ Separator: top=False (conecta ao input), bottom=True (separa das opções)
      - Benefícios:
        ✓ UX melhorada: usuário pode rapidly testar ambas as direções
        ✓ Integração com InputFieldsWidget.set_values/get_values()
        ✓ Sem estado: apenas alterna valores, não persiste
      - Risco: Baixo
        ✓ Ação não-destrutiva, simples reversão
        ✓ Sem persistência (não salva estado do botão)
    1.6.6.2
    - CORREÇÃO PROFUNDA: Gap Entre AppBar e Itens (enable_scroll=False/True)
      - Problema Raiz Identificado: Falta de stretch ao final do _inner_layout
        ✓ Com scroll: ScrollWidget + itens não preenchiam espaço, deixava gap
        ✓ Sem scroll: Itens diretos no layout sem stretch = espaço vazio abaixo
      - Arquitetura do Problema:
        ✓ Com scroll:
          - _inner_layout contém: AppBar + ScrollWidget
          - Itens dentro do ScrollWidget via container_layout
          - SEM stretch: ScrollWidget fica pequeno (tamanho do conteúdo)
        ✓ Sem scroll:
          - _inner_layout contém: AppBar + item1 + item2 + item3
          - SEM stretch: Espaço vazio abaixo dos itens
      - Solução: Adicionar addStretch() ao final em AMBOS os casos
        ✓ Com scroll: self._inner_layout.addStretch() após add_layout_as_content()
        ✓ Sem scroll: self._inner_layout.addStretch() após adicionar itens
        ✓ Resultado: Itens compactos no topo, stretch preenche resto
      - Arquivos Modificados:
        ✓ resources/widgets/MainLayout.py (método add_items())
          - Linha ~144: Added addStretch() para caso com scroll
          - Linha ~155: Added addStretch() para caso sem scroll
      - Plugins Afetados:
        ✓ ReplaceInLayouts (enable_scroll=False): GAP ELIMINADO ✓
        ✓ DroneCoordinates (enable_scroll=True): Layout mais robusto ✓
        ✓ GenerateTrailPlugin (enable_scroll=True): Layout mais robusto ✓
      - Como Funciona:
        ✓ Stretch é um spacer invisível com sizePolicy Expanding
        ✓ Mantém itens compactos no topo, preenchendo espaço vertical restante
        ✓ Consistente com ou sem scroll: mesma estrutura visual
      - Benefícios:
        ✓ Gap entre AppBar e itens eliminado completamente
        ✓ Layouts mais previsíveis: itens sempre no topo mesmo com resize
        ✓ UX consistente: mesmo comportamento em todos plugins
        ✓ Robustos: funciona com qualquer quantidade de itens
      - Risco: Muito Baixo
        ✓ addStretch() é pattern standard em Qt
        ✓ Não afeta componentes existentes (apenas melhora spacing)
        ✓ Reversível se houver comportamento indesejado
    1.6.6.1
    - CORREÇÃO: Espaçamento Inconsistente Entre AppBar e Itens (enable_scroll=False)
      - Problema: ReplaceInLayouts exibia gap grande entre AppBar e primeiro item
        ✓ Causa: _inner_layout não tinha setSpacing() quando enable_scroll=False
        ✓ Com scroll: container_layout.setSpacing(Styles.LAYOUT_V_SPACING) definido
        ✓ Sem scroll: _inner_layout.setSpacing() nunca era chamado
      - Solução: Adicionar setSpacing() em MainLayout.__init__() SEMPRE
        ✓ Linha 51: self._inner_layout.setSpacing(Styles.LAYOUT_V_SPACING)
        ✓ Agora consistent: spacing=2px com ou sem scroll
      - Arquivos Modificados:
        ✓ resources/widgets/MainLayout.py
      - Plugins Afetados (Corrigidos):
        ✓ ReplaceInLayoutsDialog: enable_scroll=False (agora com espaçamento correto)
      - Benefícios:
        ✓ Espaçamento visual consistente em todos os plugins
        ✓ Melhor UX: gaps uniformes entre componentes
      - Risco: Baixo
        ✓ change é minimal e isolada (apenas 1 linha adicionada)
        ✓ Não afeta lógica, apenas espaçamento visual


  1.6.5 2026/03/08
    1.6.5.9
    - ENCAPSULAMENTO DE SCROLL: MainLayout gerencia ScrollWidget internamente
      - Objetivo: Separação de responsabilidades - plugins NÃO tocam em scroll
      - ARQUITETURA NOVA:
        ✓ MainLayout.__init__(enable_scroll=True/False) cria ScrollWidget internamente
        ✓ Plugin NÃO importa ScrollWidget (encapsulado)
        ✓ Plugin NÃO usa self.scroll_container (não existe mais)
        ✓ add_items() roteia automaticamente:
          - Se scroll habilitado: itens adicionados ao scroll
          - Se scroll desabilitado: itens adicionados ao inner_layout
      - MODIFICAÇÕES:
        ✓ MainLayout.py: Added enable_scroll parameter, internal _scroll creation, auto-routing in add_items()
          - Added import ScrollWidget
          - Modified __init__ to accept enable_scroll parameter
          - Modified add_items() to decide routing based on self._scroll
          - Updated addWidget/addLayout methods for scroll support
        ✓ WidgetFactory.py: create_main_layout() passes enable_scroll, returns MainLayout only
          - Removed tuple unpacking (no longer returns (layout, scroll))
          - Removed ScrollWidget import (no longer needed)
        ✓ BasePlugin.py: Removed tuple unpacking, simplified assignment
          - Removed self.scroll_container attribute
          - Changed: self.layout, self.scroll_container = result → self.layout = result
        ✓ DroneCoordinates.py: Uses add_items() instead of manual content_layout
          - Removed QVBoxLayout import
          - Removed content_layout creation
          - Replaced with: self.layout.add_items([folder_layout, opts_layout, ...])
        ✓ GenerateTrailPlugin.py: Uses add_items() instead of manual content_layout
          - Removed QVBoxLayout import
          - Replaced manual scroll_container.add_layout_as_content() call
        ✓ CopyAttributesPlugin.py: Uses add_items() instead of manual content_layout
          - Removed QVBoxLayout import
          - Replaced manual scroll_container.add_layout_as_content() call
      - BENEFÍCIOS:
        ✓ Separação de responsabilidades: MainLayout gerencia scroll, plugins apenas adicionam itens
        ✓ Encapsulamento: scroll é detalhe interno não visível ao plugin
        ✓ Simplificação de código: plugins removem 10+ linhas de setup de scroll
        ✓ Reutilizabilidade: MainLayout funciona com ou sem scroll transparentemente
        ✓ Testabilidade: lógica de scroll concentrada em um lugar
        ✓ Manutenibilidade: mudanças no scroll não afetam plugins
      - RISCO: Baixo
        ✓ Behavior mantido: items ainda vão para scroll ou layout dependendo de enable_scroll
        ✓ Tests: DroneCoordinates, GenerateTrailPlugin, CopyAttributesPlugin funcionando
    1.6.5.8
    - APLICAÇÃO DE ESTILOS: InputFieldsWidget + SimpleButtonWidget
      - Renomeado FullScreenButtonWidget → SimpleButtonWidget (nome mais apropriado)
        ✓ Arquivo: resources/widgets/SimpleButtonWidget.py
      - Criado Styles.input_fields_widget(): estilo completo para InputFieldsWidget
        ✓ QLabel: rótulos padronizados
        ✓ QLineEdit: entrada de texto com borders, focus e disabled states
        ✓ QSpinBox: número inteiro com botões up/down de tamanho reduzido
        ✓ QDoubleSpinBox: número decimal com botões up/down de tamanho reduzido
        ✓ Todos com hover, focus e disabled states
      - Criado Styles.simple_button_widget(): estilo para SimpleButtonWidget
        ✓ Degradado primário, hover e pressed states
        ✓ Padrão: mesmo estilo dos botões de ação (Run, Close, etc)
      - CORREÇÃO: Bug de botões spinbox muito grandes em Styles.main_application()
        ✓ QDoubleSpinBox::up-button/down-button: 16px → 14px width, height: 8px
        ✓ Adicionado: padding: 0px; margin: 0px (antes faltava)
      - Integração WidgetFactory:
        ✓ create_input_fields_widget(): adiciona estilo input_fields_widget()
        ✓ create_full_screen_button() → create_simple_button(): adiciona estilo simple_button_widget()
      - Atualizado DroneCoordinates._build_ui():
        ✓ Referência renomeada: self.full_button → self.simple_button
        ✓ Comentário: "FullScreenButtonWidget" → "SimpleButtonWidget"
      - Benefícios:
        ✓ Estilos centralizados em Styles.py (manutenibilidade)
        ✓ Botões spinbox menores (visual melhorado)
        ✓ Consistência visual com tema do software
        ✓ Fácil customização via Styles
    1.6.5.7
    - TESTE: Novos widgets para DroneCoordinates
      - Criado InputFieldsWidget: widget com múltiplos campos de input (text/int/float) via dicionário
        ✓ Arquivo: resources/widgets/InputFieldsWidget.py
        ✓ Recebe dict com configurações de campos: {'chave': {'title': ..., 'type': ..., 'default': ...}}
        ✓ Métodos: get_values(), set_values(), get_value(key), set_value(key, value)
        ✓ Sinal: valuesChanged emite dict com valores atualizados
      - Criado FullScreenButtonWidget: botão que ocupa toda a tela
        ✓ Arquivo: resources/widgets/FullScreenButtonWidget.py
        ✓ Simples e reutilizável
      - Integrado WidgetFactory com métodos:
        ✓ create_input_fields_widget(fields_dict, ...) → retorna (layout, widget)
        ✓ create_full_screen_button(text, ...) → retorna (layout, widget)
      - Adicionado ao DroneCoordinates._build_ui() para teste
        ✓ InputFieldsWidget com 3 campos exemplo: nome (text), idade (int), altura (float)
        ✓ FullScreenButtonWidget com callback que loga ao clicar
        ✓ Ambos adicionados ao content_layout (dentro do scroll)
      - Status: TESTE (pode ser removido facilmente)
    1.6.5.6
    - CENTRALIZAÇÃO DE PERSISTÊNCIA: Todas as preferências via BasePlugin.closeEvent()
      - Objetivo: Evitar duplicação de _save_prefs() espalhadas em pipelines
      - Mudanças em BasePlugin.closeEvent():
        ✓ JÁ chama _persist_window_size() antes de _save_prefs()
        ✓ JÁ é a ÚNICA place onde preferências são salvas ao fechar a janela
      - Removido de DroneCoordinates._on_pipeline_finished():
        ✓ Linha que chamava self._save_prefs() após processamento
        ✓ Agora preferências só salvam em closeEvent (ao fechar a janela)
      - Removido de GenerateTrailPlugin._on_pipeline_finished():
        ✓ Linha que chamava self._save_prefs() após processamento
      - Removido de CopyAttributesPlugin.execute_tool():
        ✓ Chamada desnecessária de _save_prefs() antes da execução
      - Atualizado em GenerateTrailPlugin._save_prefs():
        ✓ Adicionado: self.preferences['window_width'] = self.width()
        ✓ Adicionado: self.preferences['window_height'] = self.height()
      - Atualizado em CopyAttributesPlugin._save_prefs():
        ✓ Adicionado: data['window_width'] = self.width()
        ✓ Adicionado: data['window_height'] = self.height()
      - DroneCoordinates já tinha window_width/height em _save_prefs() ✓
      - Padrão final:
        ✓ closeEvent() → _persist_window_size() → _save_prefs()
        ✓ Cada plugin implementa _save_prefs() com window_width/height
        ✓ Sem duplicação: salva uma única vez ao fechar a janela
        ✓ Consistente: todos plugins salvam tamanho em _save_prefs()
      - Benefícios:
        ✓ Single responsibility: closeEvent() é o ÚNICO lugar de persistência
        ✓ Sem race conditions: não há múltiplos saves simultâneos
        ✓ Dados sincronizados: tamanho da janela sempre incluído
        ✓ Mais limpeza de código: sem _save_prefs() espalhadas
    1.6.5.5
    - CONSOLIDAÇÃO UNIVERSAL: FileSelectorWidget → SelectorWidget
      - Objetivo: Eliminar duplicação, unificar toda seleção de paths (pastas/arquivos/salvamento)
      - Renomeação: PathSelectorWidget → SelectorWidget (mais apropriado para universalidade)
      - Mudanças:
        ✓ FileSelectorWidget.py: ELIMINADO (redundante)
        ✓ PathSelectorWidget.py: RENOMEADO para SelectorWidget.py
        ✓ SelectorWidget.py: Modos folder, folders, file, files, save todos integrados
        ✓ WidgetFactory.create_save_file_selector(): usa SelectorWidget em vez de FileSelectorWidget
        ✓ Todos métodos create_*_selector() agora retornam SelectorWidget
      - Compatibilidade retrógrada:
        ✓ PathSelectorWidget = SelectorWidget (alias em SelectorWidget.py)
        ✓ Código antigo usando PathSelectorWidget continua funcionando
        ✓ Mode constants: SelectorWidget.MODE_FOLDER, MODE_FILE, MODE_SAVE, etc
      - Remoção em Styles.py:
        ✓ file_selector_widget(): ELIMINADO (merged com path_selector_widget)
        ✓ Ambos usam agora: Styles.path_selector_widget()
      - Benefícios:
        ✓ Single responsibility: 1 widget universal para tudo
        ✓ Menos código duplicado: sem dois widgets quasi-idênticos
        ✓ UX consistente: mesmo padrão visual/comportamento em todos selectors
        ✓ Manutenibilidade: mudanças em 1 único widget
        ✓ Menos imports: plugins precisam de 1 menos dependência
      - Integração com plugins:
        ✓ DroneCoordinates: cria_path_selector() → retorna SelectorWidget
        ✓ GenerateTrailPlugin: create_save_file_selector() → retorna SelectorWidget
        ✓ APIs compatíveis: get_paths(), get_file_path(), set_file_path(), is_enabled()
    1.6.5.4
    - SIMPLIFICAÇÃO: REMOÇÃO DE OBRIGATORIEDADE DE TAMANHOS EM PLUGINS
      - Objetivo: Aproveitar persistência de tamanho para remover parâmetros repetitivos
      - Mudanças em BasePlugin.init():
        ✓ Removidos parâmetros: min_width, min_height
        ✓ Padrão fixo: 300x300 para todas as ferramentas (persistido em prefs)
        ✓ Assinatura simplificada: init(tool_key, class_name, load_settings_prefs, build_ui)
      - Mudanças em BasePlugin._build_ui():
        ✓ Removidos parâmetros: min_width, min_height
        ✓ Agora: setMinimumSize(300, 300) fixado em BasePlugin
        ✓ Tamanho persistido: cada ferramenta abre com seu tamanho anterior
      - Mudanças em BasePlugin._restore_window_size():
        ✓ Removidos parâmetros: default_width, default_height
        ✓ Padrão fixo: 300x300 se não houver preferência salva
      - Refatoração de plugins:
        ✓ DroneCoordinates: removidos min_width=250, min_height=250
        ✓ CopyAttributesPlugin: removidos min_width=320, min_height=450
        ✓ SettingsPlugin: não tinha parâmetros (mantém o padrão)
      - Benefícios:
        ✓ Menos parâmetros por plugin: interface simplificada
        ✓ Consistência global: todos plugins 300x300 mínimo
        ✓ Persistência automática: usuário define tamanho, é restaurado sempre
        ✓ Menos duplicação: padrão centralizado em BasePlugin
      - Padrão emergente:
        ✓ 300x300: tamanho mínimo consistente e persistido
        ✓ Cada ferramenta redimensiona e salva sua preferência
        ✓ NoNeed pra plugins definirem tamanhos customizados
    1.6.5.3
    - REFATORAÇÃO DE ARQUITETURA: SCROLL COMO RESPONSABILIDADE DO WIDGETFACTORY
      - Objetivo: Separação clara de responsabilidades entre WidgetFactory e plugins
      - Padrão anterior: Plugins criavam ScrollWidget diretamente em _build_ui()
      - Padrão novo: WidgetFactory.create_main_layout() gerencia scroll como parâmetro
      - Mudanças em WidgetFactory.create_main_layout():
        ✓ Novo parâmetro: enable_scroll: bool = False
        ✓ Se enable_scroll=False: retorna MainLayout (comportamento original)
        ✓ Se enable_scroll=True: retorna tuple (MainLayout, ScrollWidget)
        ✓ Adicionado import: ScrollWidget
      - Mudanças em BasePlugin._build_ui():
        ✓ Novo parâmetro: enable_scroll: bool = False
        ✓ Passa enable_scroll para create_main_layout()
        ✓ Se scroll habilitado: atribui (self.layout, self.scroll_container)
        ✓ Se sem scroll: atribui self.layout (compatível com plugins antigos)
      - Refatoração de DroneCoordinates:
        ✓ Chamada: super()._build_ui(..., enable_scroll=True)
        ✓ Removido: lógica manual de ScrollWidget
        ✓ Removido import: ScrollWidget (agora via WidgetFactory)
        ✓ Simplificação: scroll_container recebido via herança, apenas add_layout_as_content()
      - Benefícios:
        ✓ Single Responsibility: WidgetFactory responsável por scroll
        ✓ Reutilização: enable_scroll=True funciona em qualquer plugin
        ✓ Manutenibilidade: mudanças em scroll só precisam de 1 lugar
        ✓ Testabilidade: WidgetFactory encapsula lógica de scroll
      - Compatibilidade: Plugins sem enable_scroll continuam funcionando (padrão False)
    1.6.5.2
    - PADRONIZAÇÃO CENTRALIZADA DE ESTILOS (Styles.py)
      - Adicionadas constantes de altura:
        ✓ INPUT_HEIGHT = 24px (QLineEdit, QComboBox, etc)
        ✓ BUTTON_HEIGHT = 24px (QPushButton padrão)
        ✓ CHECKBOX_SIZE = 12px (checkbox size)
        ✓ RADIO_SIZE = 12px (radio button size)
      - Novos métodos de estilo por WIDGET (não por item QGIS):
        ✓ path_selector_widget() - PathSelectorWidget com radios internos
        ✓ file_selector_widget() - FileSelectorWidget com checkbox
        ✓ layer_input_widget() - LayerInputWidget com combo + checkbox
        ✓ radio_button_grid_widget() - RadioButtonGridWidget grid exclusivo
        ✓ bottom_action_buttons_widget() - BottomActionButtonsWidget botões inferiores
      - Atualizado WidgetFactory:
        ✓ create_qml_selector() → Styles.file_selector_widget()
        ✓ create_save_file_selector() → Styles.file_selector_widget()
        ✓ create_layer_input() → Styles.layer_input_widget()
        ✓ create_radio_button_grid() → Styles.radio_button_grid_widget()
        ✓ create_path_selector() → Styles.path_selector_widget()
        ✓ create_bottom_action_buttons() → Styles.bottom_action_buttons_widget()
      - Removido setStyleSheet() manual em BottomActionButtonsWidget
      - Centralização completa: todos widgets recebem estilos via WidgetFactory
      - Paleta de cores com 5 estados: normal, hover, pressed, disabled, focused
      - Estrutura reutilizável para novos widgets
    1.6.5.1
    - PERSISTÊNCIA DE TAMANHO DE JANELA AUTOMÁTICA
      - BasePlugin.closeEvent() override: salva window_width/window_height automaticamente
      - BasePlugin._restore_window_size(): restaura tamanho ao abrir cada ferramenta
      - Cada ferramenta abre com seu próprio tamanho específico
      - Padrão: min_width/min_height usado como fallback se não houver preferências
      - Benefícios:
        ✓ Separação de responsabilidades: BasePlugin gerencia persistência
        ✓ Reutilização de código: todos plugins herdam automaticamente
        ✓ UX melhorada: janela abre no tamanho que o usuário deixou
        ✓ Sem necessidade de override em cada plugin (implementado via herança)
      - Integração com plugins:
        ✓ DroneCoordinates: inclui window_width/height em _save_prefs()
        ✓ GenerateTrailPlugin, CopyAttributes, SettingsPlugin: funcionam via herança
        ✓ Padrão reutilizável para novos plugins

    1.6.5.0
    - REFATORAÇÃO COMPLETA DE LAYOUT E SPACING
      - Variáveis globais em Styles.py:
        ✓ ITEM_HEIGHT = 12px (altura padrão de cada elemento)
        ✓ ITEM_SPACING = 1px (espaçamento entre itens)
        ✓ CONTENT_PADDING = 2px (padding do conteúdo)
        ✓ LAYOUT_V_SPACING = 2px (spacing vertical padrão)
        ✓ LAYOUT_H_SPACING = 0px (spacing horizontal)
        ✓ calc_checkbox_grid_height() - calcula altura dinâmica de grids
      - Correção de widgets:
        ✓ CollapsibleParametersWidget: margins/spacing padronizados
        ✓ create_checkbox_grid(): setFixedHeight(12px) em cada checkbox
        ✓ create_save_file_selector/create_qml_selector: spacing=2px (não 10px)
      - ScrollWidget customizada criada:
        ✓ QScrollArea com estilo integrado ao tema
        ✓ Scrollbars animadas (cor primária com hover/pressed)
        ✓ Implementação limpa e reutilizável
      - DroneCoordinates refatorada:
        ✓ Scroll integrado para conteúdo principal
        ✓ Botões de ação fixos na base (fora do scroll)
        ✓ Layout sem lacunas: apenas 2px entre seções
      - MainLayout melhorado:
        ✓ Métodos get_size() / set_size(width, height) para gerenciar dimensões
        ✓ Spacing interno = LAYOUT_V_SPACING (2px)
      - Resultado: Layout compacto, consistente e sem "jumping"


  1.6.4 2026/03/06
    1.6.4.4
    - REFATORAÇÃO DroneCoordinates UI
      - PathSelectorWidget: altura reduzida 24px, margins 0,0,0,0, spacing 2
      - 3 CollapsibleParametersWidget groups com estado persistido:
        ✓ "Opções" (expandido por padrão): 3 checkboxes (recursive, merge, photos)
        ✓ "Salvamento" (colapsado): 2 path selectors (save_points, save_track)
        ✓ "Estilos" (colapsado): 2 QML selectors (style_track, apply_style)
      - Preferências: opts_expanded, save_expanded, styles_expanded persistidas em load_tool_prefs/save_tool_prefs
    1.6.4.3
    - CORREÇÃO CollapsibleParametersWidget refresh() bug
      - Problema: sizeHint() calculado antes de layout eventos finalizarem → conteúdo invisível ao expandir de prefs
      - Solução: QTimer.singleShot(0, _do_refresh) adia cálculo para próximo ciclo de eventos
      - Implementação: refresh() chamado após adicionar conteúdo em cada collapsible
      - Resultado: collapsibles expandem com conteúdo visível (sem glitch)
    1.6.4.2
    - PERSISTÊNCIA DE GEOMETRIA DA JANELA
      - BasePlugin._load_window_geometry(): restora width/height de preferências via self.resize()
      - BasePlugin._save_window_geometry(): persiste width/height em preferências via save_tool_prefs()
      - BasePlugin.closeEvent(): override salva geometria + prefs automaticamente ao fechar
      - Integração init(): _load_prefs() → _load_window_geometry() → showEvent()
      - Padrão: Todos plugins que herdam BasePluginMTL ganham persistência automática
      - Keys: window_width, window_height salvos em load_tool_prefs(TOOL_KEY)
    1.6.4.1
    - REFATORAÇÃO DE CLASSES para CamelCase
      - Classes renomeadas seguindo padrão Python: nomes descritivos em PascalCase
      - Movimentação de classes para melhor organização arquitetônica
      - Preservada retrocompatibilidade em imports críticos